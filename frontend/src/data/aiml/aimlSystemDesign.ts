import { emptyContent, type ArchitectureTopic } from '../systemDesignTypes';
import { toArticle } from './types';

export const aimlSystemDesignTopics: ArchitectureTopic[] = [
  {
    id: "aiml-mlsd-online-serving",
    title: "Online Model Serving",
    description: "Latency, scaling, and safe rollout for real-time ML.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Radio",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 0,
    content: emptyContent({ overview: "Latency, scaling, and safe rollout for real-time ML." }),
    article: toArticle([
      { type: 'heading', level: 2, text: "Requirements framing" },
      { type: 'paragraph', text: "Clarify **latency SLO**, **throughput**, **freshness** of models and features, and **failure modes** (degraded default, cached scores, shadow traffic)." },
      { type: 'heading', level: 2, text: "Architecture sketch" },
      { type: 'list', ordered: false, items: ["**Feature store** for low-latency, consistent online features.","**Model server** with autoscaling and warm pools.","**Traffic splitting** for canaries and rollbacks.","**Observability**: latency histograms, error budgets, drift monitors."] },
      { type: 'heading', level: 2, text: "Tradeoffs" },
      { type: 'paragraph', text: "Bigger models improve quality but hurt tail latency—optimize the percentile your product actually cares about." }
    ]),
  },
  {
    id: "aiml-mlsd-batch-and-streaming",
    title: "Batch vs Streaming Features",
    description: "How data movement shapes ML system design.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "GitMerge",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 1,
    content: emptyContent({ overview: "How data movement shapes ML system design." }),
    article: toArticle([
      { type: 'heading', level: 2, text: "When batch wins" },
      { type: 'paragraph', text: "Periodic training, large backfills, and cheaper joins at rest." },
      { type: 'heading', level: 2, text: "When streaming wins" },
      { type: 'paragraph', text: "Near-real-time decisions, fraud, recommendations that react quickly." },
      { type: 'heading', level: 2, text: "Bridge pattern" },
      { type: 'paragraph', text: "Stream ingestion with compacted snapshots for training, plus point lookups for online inference." }
    ]),
  },
  {
    id: "aiml-mlsd-data-sources-connectors",
    title: "Data Sources & Connectors",
    description: "Integrate structured, unstructured, and streaming data sources into ML pipelines.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Link2",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 2,
    content: emptyContent({ overview: "Integrate structured, unstructured, and streaming data sources into ML pipelines." }),
    article: toArticle([
      { type: 'paragraph', text: "Production ML systems rarely sit on a single tidy table. They ingest operational databases, object stores full of documents, event streams, SaaS APIs, and ad hoc files. Connectors are the boundary between those heterogeneous sources and your pipelines. The architectural question is not only how to read data once, but how to keep it synchronized with acceptable lag, cost, and correctness guarantees." },
      { type: 'paragraph', text: "Common integration patterns include change data capture for relational systems, webhooks or push subscriptions for event-driven sources, and scheduled polling when APIs or legacy systems only expose pull semantics. CDC minimizes load on the primary database and captures inserts, updates, and deletes in order, which matters when downstream features depend on temporal consistency. Polling is simpler to reason about but amplifies cost and can miss rapid changes between polls unless you track high watermarks carefully. Webhooks reduce latency but require durable ingress, signature verification, and replay handling when delivery fails." },
      { type: 'paragraph', text: "Structured data usually arrives with explicit schemas; unstructured data such as PDFs, HTML, images, and chat logs requires parsing, normalization, and often enrichment before it is model-ready. Schema evolution is a first-class concern: additive columns are easy, but renames, type changes, and nested structure drift break naive consumers unless you version contracts, use compatibility checks in CI, and maintain transformation layers that can tolerate multiple schema generations during rollouts." },
      { type: 'paragraph', text: "Teams often land raw data in a data lake for flexibility and reserve a warehouse or curated marts for query performance and governance. Managed connector frameworks such as Airbyte and Fivetran accelerate time to value with prebuilt sources, scheduling, and monitoring, while custom connectors win when you need proprietary protocols, aggressive cost control, or tight coupling to internal security models. The trade-off is operational burden versus control: frameworks reduce engineering toil but can obscure failure modes, vendor-lock certain semantics, and charge per row or per connector in ways that do not scale for very high cardinality streams." },
      { type: 'qa', question: "When would you prefer CDC over nightly batch extracts from an OLTP database?", category: "Architecture", answer: "CDC is preferable when you need fresher features or analytics with lower load on the production database, when deletes matter for correctness, or when row-level ordering is important for downstream deduplication. Nightly batches are simpler and cheaper for low freshness requirements and small tables, but they stress the database with large scans and can miss intraday changes unless you add incremental logic anyway." },
      { type: 'qa', question: "How do you integrate a SaaS API that rate-limits aggressively and returns paginated JSON?", category: "Integration", answer: "Use a durable queue or workflow engine so retries are idempotent, persist cursors or updated-at watermarks per object type, backoff with jitter on 429 responses, and shard work across workers respecting per-tenant limits. Normalize responses into a staging schema, validate against a contract, and log raw payloads briefly for forensic replay while respecting retention policies." },
      { type: 'qa', question: "What are the main trade-offs between buying a connector platform and building custom ingestion?", category: "Trade-offs", answer: "Purchased platforms reduce engineering time, ship monitoring and many sources, and centralize connector maintenance. Custom builds optimize for proprietary formats, strict latency or cost targets, and security constraints such as air-gapped deployments. The risk of platforms is opaque failure handling, pricing that grows with volume, and limited control over backpressure semantics; custom code trades faster iteration on features for ongoing maintenance every time an upstream API changes." }
    ]),
  },
  {
    id: "aiml-mlsd-document-processing-chunking",
    title: "Document Processing & Chunking",
    description: "Parse, split, and prepare documents for embedding and retrieval.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Scissors",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 3,
    content: emptyContent({ overview: "Parse, split, and prepare documents for embedding and retrieval." }),
    article: toArticle([
      { type: 'paragraph', text: "Retrieval-augmented generation and similar workflows depend on turning documents into retrievable units. Chunking is not merely splitting text at a character budget; it is a compression of structure and semantics into segments that an embedding model can represent faithfully and that a retriever can rank against queries. Poor chunking produces fragments that lack local context, duplicate content across neighbors, or split tables and code blocks in ways that destroy parseability." },
      { type: 'paragraph', text: "Parsing paths differ by format. HTML benefits from DOM-aware extraction that strips boilerplate and preserves headings as metadata. Markdown often carries explicit hierarchy you can use for section-aware splits. PDFs are hardest: layout engines, multi-column pages, headers, and footers require specialized libraries or OCR when text is embedded as images, and you should store page numbers and bounding boxes when downstream citations matter. Regardless of format, extracting titles, section paths, source URLs, authorship, and timestamps as metadata improves filtering and reranking later." },
      { type: 'paragraph', text: "Chunking strategies include fixed-size windows with overlap, recursive splitting that prefers natural boundaries such as paragraphs and sentences, and semantic chunking that merges or splits based on embedding similarity between adjacent sentences. Fixed windows are predictable and cheap but can bisect lists and equations. Recursive approaches respect linguistic boundaries at the cost of variable chunk sizes. Semantic chunking can yield more coherent units but adds latency and complexity because it requires an embedding pass or a smaller model to score coherence before the main embedding job." },
      { type: 'paragraph', text: "Overlap reduces boundary effects where a concept spans two chunks, but excessive overlap inflates storage, increases redundant hits in retrieval, and can bias vector indexes toward repetitive boilerplate. Chunk size trades recall against precision: very small chunks match specific facts but lose surrounding constraints; very large chunks preserve context but dilute the embedding and may exceed model input limits during reranking. Production systems often combine hierarchical chunking (parent documents with child passages), deduplication at index time, and evaluation with human-judged retrieval metrics rather than guessing sizes from defaults alone." },
      { type: 'qa', question: "Why might recursive splitting outperform fixed-size chunking for technical documentation?", category: "RAG", answer: "Technical docs contain lists, code fences, and cross-references that fixed windows cut arbitrarily. Recursive splitting walks a hierarchy of separators so chunks tend to end at paragraph or sentence boundaries, keeping syntactic units intact. That improves both human readability in citations and embedding quality because the model sees complete instructions rather than half a code block." },
      { type: 'qa', question: "What metadata would you attach to each chunk before indexing?", category: "Implementation", answer: "At minimum store document id, stable chunk id, section title or path, content type, language, creation or crawl time, and source URI. For PDFs add page number; for APIs add tenant or product id if multi-tenant. Optional enrichments include token count, quality flags from parsing, ACL scope for authorization at query time, and parent chunk ids for hierarchical retrieval." },
      { type: 'qa', question: "How do you choose chunk size and overlap for a new domain?", category: "Trade-offs", answer: "Start from embedding model context limits and reranker budgets, then run offline experiments with labeled question-chunk relevance pairs or LLM judges on a held-out query set. Measure nDCG or recall at k across chunk sizes and overlaps, and inspect failure modes such as merged topics or over-split procedures. Balance index size and cost: larger overlap improves recall at boundaries but multiplies vectors and can add noisy duplicates unless you deduplicate or down-weight them." }
    ]),
  },
  {
    id: "aiml-mlsd-embedding-generation-pipeline",
    title: "Embedding Generation Pipeline",
    description: "Design scalable pipelines that convert raw data into vector representations.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Binary",
    color: "bg-teal-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 4,
    content: emptyContent({ overview: "Design scalable pipelines that convert raw data into vector representations." }),
    article: toArticle([
      { type: 'paragraph', text: "An embedding pipeline turns text, images, or multimodal records into dense vectors used for search, clustering, and feature inputs. Architecture centers on throughput, idempotency, and the lifecycle of models: you must be able to re-embed corpora when models change, handle late-arriving documents, and avoid double-charging GPU hours on duplicate work. Treat embeddings as derived artifacts versioned alongside the model name, dimensionality, and preprocessing recipe." },
      { type: 'paragraph', text: "Model selection balances quality, latency, dimensionality, license terms, and whether you need multilingual or domain adaptation. Open models let you self-host for data residency; hosted APIs simplify operations but couple cost to volume. Dimensionality affects index size and query speed: higher dimensions can improve separability but increase RAM and distance-compute cost; some teams train or apply PCA after the fact, though that is less common with modern API embeddings that ship fixed sizes." },
      { type: 'paragraph', text: "Batch generation maximizes GPU utilization by packing sequences to pad-efficient shapes and amortizing kernel launch overhead; it fits offline backfills and periodic reindex jobs. Streaming or micro-batch paths prioritize freshness for user-visible documents and pair well with queue consumers that scale with ingestion rate. Hybrid designs batch within short windows to smooth spikes while keeping p95 lag bounded." },
      { type: 'paragraph', text: "Normalize vectors when your distance metric assumes unit length, which is typical for cosine similarity workflows. Incremental updates require a stable document-to-chunk mapping so you re-embed only changed segments, using content hashes to skip unchanged work. Embedding drift arises when upstream text edits, the model is upgraded, or the data distribution shifts; mitigate it with dual-write periods, versioned vector namespaces, gradual reranking experiments, and monitoring retrieval quality against golden queries rather than assuming static vectors stay optimal forever." },
      { type: 'qa', question: "How would you architect a pipeline to re-embed ten billion tokens after swapping embedding models?", category: "Architecture", answer: "Partition the corpus by shard keys, enqueue work idempotently with content hashes, and run GPU workers that write to a new index namespace keyed by model version. Keep the old index serving until the new one passes quality gates on a benchmark query set, then flip a routing flag and retire the old namespace asynchronously. Track cost and throughput per shard to right-size clusters and use spot or preemptible instances only for fault-tolerant stages with checkpointing." },
      { type: 'qa', question: "What batching considerations matter most for embedding throughput?", category: "Performance", answer: "Dynamic padding or bucketing by length reduces wasted compute on short sequences. Micro-batching should respect GPU memory headroom for peak lengths. Avoid per-item synchronous API calls when self-hosting; instead pipeline tokenization, GPU forward, and vector upload with overlapping stages. Monitor queue lag and batch formation time because overly large batches improve MFU but can violate freshness SLAs." },
      { type: 'qa', question: "How do you decide between a general-purpose embedding model and a domain-finetuned one?", category: "Model Selection", answer: "Start with a strong general model and measure retrieval precision on in-domain queries. Finetune or use adapters when you have labeled relevance data or consistent vocabulary mismatch. Finetuning improves in-domain clustering and recall but raises MLOps cost, risks catastrophic forgetting on out-of-domain queries, and forces a reindex. If data is sensitive or small, few-shot rerankers on top of a general retriever may be enough without changing the base embedder." }
    ]),
  },
  {
    id: "aiml-mlsd-data-versioning-lineage",
    title: "Data Versioning & Lineage",
    description: "Track data provenance, schema evolution, and reproducibility across experiments.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "GitCommitHorizontal",
    color: "bg-emerald-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 5,
    content: emptyContent({ overview: "Track data provenance, schema evolution, and reproducibility across experiments." }),
    article: toArticle([
      { type: 'paragraph', text: "Machine learning experiments are only reproducible if you can reconstruct the exact inputs and code that produced a model. Data versioning tools such as DVC treat large artifacts as content-addressed blobs while keeping lightweight metadata in Git, which makes diffs reviewable without committing terabytes. LakeFS layers git-like branching over object storage so teams can isolate ETL changes, run experiments on snapshots, and merge curated datasets with atomic promotions. Both approaches complement rather than replace database migrations and contract tests." },
      { type: 'paragraph', text: "Immutable datasets are a practical discipline: once a snapshot is used for a benchmark or regulatory submission, you never overwrite it in place. Instead you append new versions with explicit metadata including schema hash, row counts, and sampling strategy. Schema versioning should be explicit in Avro, Protobuf, or JSON Schema registries so producers and consumers negotiate compatibility rules rather than failing silently when a new column appears." },
      { type: 'paragraph', text: "Lineage graphs connect raw sources through transformations to features, training sets, and exported models. OpenLineage and similar standards help unify metadata across Spark, dbt, and orchestrators so you can answer impact questions when a source column is deprecated. The hard parts are completeness across ad hoc notebooks, third-party SaaS extracts, and manual human labels, which often become lineage gaps unless ingestion is forced through governed paths." },
      { type: 'paragraph', text: "Reproducibility still breaks when nondeterminism enters through floating-point order, distributed shuffles without fixed seeds, time-dependent splits, or silently updated vendor snapshots. Governance adds retention policies, access logs, and PII tagging so lineage is not only technical but auditable. Trade-offs include storage cost for many snapshots, slower iteration if every exploratory query must be cataloged, and engineer friction unless tooling is fast enough to become the default path." },
      { type: 'qa', question: "Why is content addressing useful for large training datasets?", category: "Reproducibility", answer: "Content hashes let you verify that two machines or two months later refer to identical bytes even if file paths differ. It prevents accidental overwrites, supports deduplication across experiments, and gives a stable identifier to log next to model checkpoints. Pair hashes with human-readable aliases or tags so scientists can discover datasets without memorizing digests." },
      { type: 'qa', question: "How does LakeFS differ from only using DVC for data versioning?", category: "Tooling", answer: "LakeFS operates at the data lake layer with branches, merges, and isolation for ETL jobs against object storage, which helps multi-team workflows on shared tables. DVC is closer to Git-centric ML projects and excels at tying specific data revisions to code commits for single-repo experiments. Many organizations use both: LakeFS for warehouse-scale isolation and DVC for project-scoped experiment bundles." },
      { type: 'qa', question: "What metadata belongs on a lineage edge between two pipeline tasks?", category: "Governance", answer: "Capture source and destination identifiers, transformation name and version, runtime environment fingerprint, start and end timestamps, row counts or byte metrics, and success status. For regulated workloads also log operator identity, purpose codes, and whether PII was transformed or masked. Rich edges make blast-radius analysis accurate when you need to invalidate downstream assets after a bad ingest." }
    ]),
  },
  {
    id: "aiml-mlsd-ingestion-orchestration",
    title: "Ingestion Orchestration",
    description: "Coordinate ETL/ELT workflows for reliable data movement at scale.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Workflow",
    color: "bg-lime-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 6,
    content: emptyContent({ overview: "Coordinate ETL/ELT workflows for reliable data movement at scale." }),
    article: toArticle([
      { type: 'paragraph', text: "ETL and ELT orchestration schedules work that lands data in lakes, warehouses, and feature stores. Airflow remains common for cron-style DAGs with rich operator ecosystems; Dagster emphasizes software-defined assets and data-aware dependencies; Prefect and similar tools focus on dynamic workflows and developer ergonomics for Python-native tasks. The right choice depends on team skills, how strongly you want assets as first-class objects, and whether you need fine-grained per-run parameterization without redeploying DAG definitions." },
      { type: 'paragraph', text: "Idempotency is the backbone of reliable ingestion. Tasks should be safe to retry: write to partitioned staging locations, merge with deterministic keys, and use upsert semantics or merge statements that collapse duplicates. Backfills are routine after logic bugs or late-arriving facts; design partitions so you can reprocess bounded date ranges without rewriting entire tables. Exactly-once end-to-end semantics are rare outside managed streaming stacks; in batch land you usually aim for effectively-once behavior via idempotent sinks and deduplication keys." },
      { type: 'paragraph', text: "DAG design should reflect failure domains: separate extract, transform, and load stages so a transform bug does not force re-pulling terabytes from an external API. Keep fan-in and fan-out explicit to avoid hidden resource contention. Retry policies should distinguish transient network errors from data errors that will never succeed without code changes, surfacing the latter to alerting channels quickly." },
      { type: 'paragraph', text: "Operations benefit from SLA timers on critical paths, data quality checks as sensors or tasks, and observability into queue depth and executor saturation. Trade-offs include orchestrator operational overhead versus glue scripts, and centralization versus team autonomy when many micro-DAGs create cross-dependencies that are hard to visualize." },
      { type: 'qa', question: "How do you make a daily warehouse load resilient to partial failures?", category: "Reliability", answer: "Stage raw extracts to durable storage before transforms so you can replay from the landing zone. Use transactional boundaries or partition swaps so consumers never see half-updated tables. Configure retries with exponential backoff for transient failures, circuit break on persistent upstream errors, and alert when quality checks fail thresholds so you can pause downstream jobs instead of propagating corrupt aggregates." },
      { type: 'qa', question: "When would you split one large DAG into several smaller ones?", category: "Architecture", answer: "Split when domains have different owners, SLAs, or failure blast radius, or when independent scaling and deployment cadences matter. Shared dependencies can be modeled with external task sensors or dataset events, but avoid fragile chains of waiting sensors that hide circular dependencies. Smaller DAGs improve clarity at the cost of more cross-DAG coordination logic to maintain." },
      { type: 'qa', question: "What runbook steps matter after a failed backfill?", category: "Operations", answer: "Verify which partitions were committed, reconcile row counts and checksums against expectations, recompute downstream incremental models in dependency order, and invalidate caches or feature materializations tied to those dates. Communicate to stakeholders if dashboards moved temporarily, document root cause, and add a regression test or quality assertion so the same logic bug cannot silently recur." }
    ]),
  },
  {
    id: "aiml-mlsd-llm-inference-engine",
    title: "LLM Inference Engine",
    description: "vLLM, TensorRT-LLM, TGI — high-throughput engines for large language models.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Cpu",
    color: "bg-amber-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 7,
    content: emptyContent({ overview: "vLLM, TensorRT-LLM, TGI — high-throughput engines for large language models." }),
    article: toArticle([
      { type: 'paragraph', text: "Modern LLM serving engines such as vLLM, TensorRT-LLM, and Text Generation Inference exist to squeeze useful tokens per second from expensive GPUs. The dominant cost after weights are loaded is memory bandwidth and attention: autoregressive decoding spends most time on matrix multiplies and on managing the key-value cache that grows linearly with sequence length for every active sequence. Engines therefore focus on memory-efficient KV storage, batching many requests together, and reducing redundant computation across users." },
      { type: 'paragraph', text: "PagedAttention virtualizes the KV cache into non-contiguous blocks similar to an OS paging allocator, reducing fragmentation when concurrent requests have different lengths. Continuous batching, sometimes called iteration-level scheduling, adds new sequences to the running batch as others finish prefill or decode steps, which improves GPU utilization compared to static batching that waits for the slowest member. Together these techniques materially raise throughput on variable-length production traffic." },
      { type: 'paragraph', text: "Speculative decoding uses a smaller draft model or n-gram heuristics to propose multiple tokens per step, which the large model verifies in parallel, trading extra compute for fewer large-model forward passes when drafts are often correct. Prefix caching reuses KV blocks for shared system prompts or tool schemas so repeated prefixes do not recompute from scratch. Quality-of-service features may include separate queues for interactive versus batch workloads, max concurrency limits, and preemption policies." },
      { type: 'paragraph', text: "Throughput and latency pull in opposite directions: large batches improve MFU but increase queueing delay for short requests. Production sizing requires percentile latency targets, not averages, and often pairs an engine with request prioritization, load shedding under overload, and autoscaling that understands GPU fragmentation." },
      { type: 'qa', question: "Why does naive static batching waste GPU capacity for LLM decoding?", category: "Performance", answer: "Static batches form a group of requests that advance in lockstep; the batch effective size shrinks as sequences finish at different lengths, leaving SIMD lanes idle. Padding to the longest sequence wastes compute on shorter ones. Continuous batching refills the batch each iteration so the GPU stays fuller, and paged KV reduces memory holes that would otherwise cap concurrency." },
      { type: 'qa', question: "Where does prefix caching help most in a typical RAG or agent deployment?", category: "Architecture", answer: "It helps when many requests share long identical prefixes such as system instructions, retrieved context templates, or repeated tool definitions. Those shared tokens amortize prefill cost across requests. It helps less when every user message is unique and placed early in the prompt with little shared structure, so measure hit rates before assuming large wins." },
      { type: 'qa', question: "What operational metrics would you watch when rolling out speculative decoding?", category: "Implementation", answer: "Track acceptance rate of draft tokens, end-to-end latency percentiles, tokens per second per dollar, and error rates from verification mismatches. Watch GPU memory for holding two models if draft is separate, and monitor tail latency if speculation adds variance. Compare quality on golden prompts because aggressive drafting can interact badly with certain sampling configurations." }
    ]),
  },
  {
    id: "aiml-mlsd-quantization-optimization",
    title: "Quantization & Optimization",
    description: "Reduce model size and latency with quantization, pruning, and distillation.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Gauge",
    color: "bg-orange-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 8,
    content: emptyContent({ overview: "Reduce model size and latency with quantization, pruning, and distillation." }),
    article: toArticle([
      { type: 'paragraph', text: "Quantization maps high-precision weights and activations to lower bit widths so models fit in less VRAM and move fewer bytes per matmul. INT8 post-training quantization is widely supported and often preserves accuracy on large models when kernels are well tuned. INT4 and mixed schemes such as GPTQ or AWQ push further by exploiting per-channel scales and sometimes protecting sensitive weight outliers. FP8 on Hopper-class hardware offers a sweet spot where dynamic range is better than INT8 with competitive throughput when tensor cores expose fast FP8 paths." },
      { type: 'paragraph', text: "GGUF and similar formats matter for CPU and consumer-GPU inference ecosystems where portability and mmap-friendly single files simplify deployment, though server-class GPU stacks more often use TensorRT engines or vendor-specific checkpoints. Quantization is not the only lever: structured pruning removes whole channels or heads with retraining or distillation to recover accuracy, and knowledge distillation trains a smaller student to mimic a teacher logits or hidden states." },
      { type: 'paragraph', text: "Quality degradation must be measured on tasks that mirror production, not only perplexity. Use held-out prompts with rubric scoring, regression suites for function calling and JSON validity, and domain-specific benchmarks for code or math. Calibration datasets for static quantization should reflect real input token distributions; mismatched calibration, for example only short prompts when production uses huge contexts, skews scales and raises error rates." },
      { type: 'paragraph', text: "Trade-offs include engineering time to integrate kernels, brittleness across hardware generations, and harder debugging when numerical issues surface as subtle hallucination increases rather than hard crashes. Some teams keep a full-precision shadow model for offline evaluation while serving quantized weights online." },
      { type: 'qa', question: "What is the difference between PTQ and QAT for LLM deployment?", category: "Optimization", answer: "Post-training quantization adjusts scales and zero points after training without gradient updates, which is fast but can clip important weights if outliers are mishandled. Quantization-aware training simulates low precision during training so weights adapt, often yielding better accuracy at aggressive bit widths at the cost of longer training cycles and more complex tooling." },
      { type: 'qa', question: "When might INT4 weight-only quantization be risky for a customer support bot?", category: "Trade-offs", answer: "Customer bots mix long context, policy-heavy language, and tool use where small logits shifts break JSON or choose wrong tools. INT4 can increase brittle failures on rare intents. If tail quality and compliance matter, validate on stratified transcripts, keep safety classifiers calibrated, and consider INT8 or FP8 for the highest-risk tenants or reserve INT4 for draft generation with verification." },
      { type: 'qa', question: "How would you build a calibration dataset for static INT8 activation quantization?", category: "Implementation", answer: "Sample realistic prompts and multimodal inputs from production logs with PII scrubbed, stratified by length, language, and feature flags. Include edge cases such as empty inputs, unicode-heavy text, and maximum-length stress tests. Run enough batches to stabilize histograms of activations, version the dataset with the model checkpoint, and re-run calibration when architecture or preprocessing changes." }
    ]),
  },
  {
    id: "aiml-mlsd-load-balancing-autoscaling",
    title: "Load Balancing & Auto-Scaling",
    description: "Distribute inference load and scale GPU resources dynamically.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Scale",
    color: "bg-rose-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 9,
    content: emptyContent({ overview: "Distribute inference load and scale GPU resources dynamically." }),
    article: toArticle([
      { type: 'paragraph', text: "LLM inference load balancers must be GPU-aware. A naive round robin ignores memory headroom: two heavy prompts landing on the same card can cause eviction or out-of-memory while another card sits idle. Better schedulers track KV cache usage estimates, batch depth, and model shard placement for tensor-parallel replicas. Session affinity matters when using continuous batching with sticky prefixes, but strict affinity can create hot spots, so hybrid policies route new work to the least loaded replica while optionally pinning long sessions." },
      { type: 'paragraph', text: "Request queuing is inevitable under bursts. You choose between rejecting with retry-after headers, degrading to shorter outputs, or queueing with explicit timeouts. Deep queues improve utilization but explode tail latency, so tiered queues for premium versus best-effort traffic are common. Autoscaling signals include queue depth, p95 prefill and decode latency, GPU utilization, and power draw proxies such as SM occupancy. Pure CPU metrics mislead because a GPU can be memory bound while reporting high utilization." },
      { type: 'paragraph', text: "Cold start includes pulling container images, loading multi-hundred-gigabyte weights from disk to VRAM, and JIT compilation for some runtimes. Warm pools of preloaded instances reduce risk at the cost of idle spend. Spot or preemptible GPUs cut cost for fault-tolerant batch inference but need checkpoint-friendly jobs and rapid failover to on-demand capacity for SLA-backed endpoints." },
      { type: 'paragraph', text: "Architectures often combine a gateway that authenticates and rate limits, a router that picks model replicas, and horizontal pod autoscaler rules tuned with custom metrics exporters from the inference server. Cost optimization trades replica count against SLOs: aggressive scale-to-zero saves money but conflicts with strict latency guarantees unless you maintain a minimum fleet during peak windows." },
      { type: 'qa', question: "Why is GPU utilization alone a weak autoscaling metric for LLM serving?", category: "Scaling", answer: "Utilization can look healthy while memory bandwidth is saturated or KV cache is full, which still causes queueing and timeouts. Conversely low average utilization can hide bursty prefill phases. Pair utilization with request queue age, batch formation rate, and latency error budget consumption to scale before users notice degradation." },
      { type: 'qa', question: "How would you design load balancing for tensor-parallel model shards?", category: "Architecture", answer: "Requests must land on complete shard groups that communicate in lockstep, so the load balancer targets replica sets rather than individual pods. Health checks should verify all shards in a group are ready. Scale replica groups as a unit and avoid splitting a user session across groups unless your client layer can transparently reconnect, which is hard for streaming sessions." },
      { type: 'qa', question: "What is the cost trade-off of keeping a warm pool versus aggressive scale-to-zero?", category: "Cost", answer: "Warm pools pay for idle GPUs and electricity but protect latency SLOs and absorb unpredictable spikes without queue buildup. Scale-to-zero minimizes baseline cost yet pays in cold starts, potential thundering herds on wake, and more complex burst handling. Hybrid approaches keep a small warm baseline, use predictive scaling from historical traffic, and burst onto spot for non-interactive workloads." }
    ]),
  },
  {
    id: "aiml-mlsd-batch-vs-realtime-inference",
    title: "Batch vs Real-time Inference",
    description: "Choose between throughput-optimized batch and latency-sensitive online serving.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Clock",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 10,
    content: emptyContent({ overview: "Choose between throughput-optimized batch and latency-sensitive online serving." }),
    article: toArticle([
      { type: 'paragraph', text: "Batch inference maximizes throughput by scheduling large volumes of inputs when latency to any single row is flexible. It fits nightly scoring, embedding backfills, evaluation harnesses, and offline feature generation for training sets. Systems typically partition inputs, run embarrassingly parallel workers with checkpointing, and write idempotent outputs to partitioned tables. Failures are retried at partition granularity without user-visible timeouts." },
      { type: 'paragraph', text: "Real-time inference serves individual or small batches of requests with tight latency budgets, often through autoscaling HTTP or gRPC services behind load balancers. It fits search ranking, fraud checks, chat assistants, and personalization where the user waits on the result. These paths optimize for tail latency, graceful degradation, and observability per request rather than aggregate rows per dollar alone." },
      { type: 'paragraph', text: "Hybrid patterns are common: embed documents asynchronously while querying synchronously against the warm index, or run a fast online model with periodic batch refresh of heavier teacher scores. Feature freshness requirements drive the split: if marketing segments can tolerate hours of lag, batch is cheaper; if fraud signals must reflect the last few seconds, you pay for streaming features and online models." },
      { type: 'paragraph', text: "SLA-driven design starts from user-visible deadlines, then budgets network, gateway, retrieval, and model time. When SLAs conflict with cost, teams introduce approximate answers, partial retrieval, or model cascades that try cheap paths first. The main trade-off is operational complexity: two stacks to maintain versus forcing batch jobs into an online path that will thrash under load or waste GPUs idle overnight." },
      { type: 'qa', question: "When is batch inference inappropriate even if average latency requirements seem loose?", category: "Architecture", answer: "It is inappropriate when decisions must reflect events that arrive continuously and stale scores cause bad outcomes, such as live bidding, safety moderation on uploads, or operational alerts. It is also wrong when feedback loops need immediate updates to avoid training on outdated labels, unless you explicitly design delayed supervision with safe defaults in between." },
      { type: 'qa', question: "What are the main downsides of running everything as real-time inference?", category: "Trade-offs", answer: "Cost scales with peak concurrency, GPUs sit underutilized off-peak, and complex autoscaling increases incident surface. Large bulk recomputes become expensive and slow compared to optimized batch frameworks. You also lose easy checkpointed replay unless you build the same durability guarantees batch systems get for free." },
      { type: 'qa', question: "How would you design a hybrid pipeline for document classification with a daily retrain?", category: "Design", answer: "Stream new documents into a queue that updates a near-real-time tier one classifier for user-facing triage, while nightly jobs retrain or finetune on accumulated labels and validate on holdouts. Promote new weights through a canary deployment with shadow scoring, then switch the online service. Keep versioned features so batch training rows align with what online inference used historically." }
    ]),
  },
  {
    id: "aiml-mlsd-multi-model-routing-fallbacks",
    title: "Multi-Model Routing & Fallbacks",
    description: "Route requests across models with fallback chains and cost optimization.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Shuffle",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 11,
    content: emptyContent({ overview: "Route requests across models with fallback chains and cost optimization." }),
    article: toArticle([
      { type: 'paragraph', text: "Production LLM applications rarely rely on a single model. Routers choose among options with different cost, latency, context limits, and capability profiles. Simple policies route by tenant tier, task type, or prompt length. More advanced routers train lightweight classifiers or use small models to predict which large model is needed, or they cascade: attempt a cheap model first and escalate only when confidence is low or validation fails." },
      { type: 'paragraph', text: "Fallback chains improve reliability when a provider returns 429 errors, when a regional endpoint fails health checks, or when output schema validation fails and a retry with a stricter model is warranted. Each hop should be bounded by deadlines so total latency does not exceed client timeouts. Circuit breakers stop hammering unhealthy backends and allow half-open probes to recover gracefully." },
      { type: 'paragraph', text: "Model cascades differ from pure fallbacks: they spend more compute only on hard inputs while keeping easy inputs cheap. A and B traffic splitting supports experimentation: route a percentage of users to a new prompt or model, compare metrics, and roll forward or back. Feature flags and consistent hashing by user id keep experiences stable across requests." },
      { type: 'paragraph', text: "Cost optimization intersects routing when you downgrade non-critical workloads to smaller models or shorten max tokens dynamically. The trade-off is engineering and observability complexity: more branches mean harder attribution of quality regressions unless you tag spans with router decisions and outcomes." },
      { type: 'qa', question: "How do circuit breakers interact with fallback chains in a multi-provider setup?", category: "Reliability", answer: "When error rates or latency against a provider exceed thresholds, the breaker opens and the router skips that provider for a cool-down period, sending traffic to the next backend in the chain. Half-open states send a trickle of probes to detect recovery without flooding a bad endpoint. Log each transition with reason codes so on-call can distinguish saturation from hard outages." },
      { type: 'qa', question: "What signals would you use for a capability-based router between a small and large LLM?", category: "Architecture", answer: "Combine cheap heuristics such as token count, language detection, and tool requirement flags with a calibrated confidence score from the small model if you use a cascade. For RAG, router inputs may include retrieval score distributions. Always include safety bucketing so high-risk topics always route to the stronger model regardless of length heuristics." },
      { type: 'qa', question: "How do you run an A/B test between two models without biasing metrics?", category: "Cost", answer: "Assign users or sessions with stable hashing to arms, keep prompts comparable, and log model identifiers with outcomes. Watch both quality metrics and spend per successful task, because a cheaper model might look good on latency while hurting conversion. Use sequential testing or frequentist power analysis to avoid peeking bias, and ensure fallback paths do not contaminate arms unless you stratify analysis." }
    ]),
  },
  {
    id: "aiml-mlsd-prompt-templates-versioning",
    title: "Prompt Templates & Versioning",
    description: "Manage prompt templates with version control and A/B testing support.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "BookTemplate",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 12,
    content: emptyContent({ overview: "Manage prompt templates with version control and A/B testing support." }),
    article: toArticle([
      { type: 'paragraph', text: "Production LLM systems rarely hard-code a single string in application code. Instead they centralize prompts in a template layer that separates static instructions from dynamic slots (user input, retrieved documents, tool outputs, locale, and safety policies). Good template systems define a schema for variables, default values, escaping rules for nested content, and compile-time checks so missing keys fail in CI rather than at runtime. That contract becomes especially important when the same logical prompt is rendered for multiple models that differ in tokenizer quirks, stop sequences, and maximum context lengths." },
      { type: 'paragraph', text: "Version control for prompts is not identical to Git alone. You typically want immutable versions tagged with metadata: model family, temperature and decoding parameters, evaluation scores on a frozen benchmark, author, and the dataset snapshot used for regression tests. A prompt registry stores these artifacts and maps logical names (for example, support_ticket_summarizer_v3) to concrete template bodies and parameter bundles. Rollback means repointing a feature flag or configuration entry to a prior registry version without redeploying code, while still retaining the ability to correlate logs with the exact prompt hash that produced each response." },
      { type: 'paragraph', text: "Few-shot examples deserve first-class versioning because they are often the highest-leverage part of a template. Managing them as structured records (example text, label, weight, source dataset row id) lets you audit provenance, prune redundant demonstrations that inflate token cost, and detect leakage from evaluation sets into the prompt. Teams sometimes maintain parallel template branches for A/B experiments, but the architecture should enforce that only one branch is active per traffic slice and that analytics join on version identifiers." },
      { type: 'paragraph', text: "Trade-offs appear between flexibility and governance. Highly dynamic templates with arbitrary string concatenation are fast to iterate but brittle; rigid templating with typed slots is safer but can slow product teams. The operational sweet spot is usually declarative templates plus automated diff review, golden-output tests on representative inputs, and a clear deprecation policy so old versions do not linger in shadow traffic or cron jobs." },
      { type: 'qa', question: "How would you structure a prompt registry so teams can iterate safely?", category: "Management", answer: "I would store immutable prompt versions with metadata such as model id, decoding parameters, benchmark results, and ownership. Production resolves a logical name to a pinned version via configuration or feature flags, with audit logs tying each response to the prompt hash. CI runs golden tests and schema checks on every change, and promotion requires review plus explicit activation rather than implicit latest pointers in code paths that matter for compliance." },
      { type: 'qa', question: "What architectural boundaries belong between templates and application code?", category: "Architecture", answer: "Templates should own wording, slot ordering, and few-shot packs, while application code supplies validated values for slots and handles retries, timeouts, and observability. Crossing that boundary cleanly avoids scattering prompt fragments in strings across services and lets you swap models or languages without rewriting business logic. Shared libraries can render templates from a central store so microservices do not fork divergent copies of the same instruction text." },
      { type: 'qa', question: "Describe a practical rollback strategy after a bad prompt deploy.", category: "Operations", answer: "Keep the previous registry version hot and addressable by id. On elevated error rates or quality regressions detected by monitors, flip the configuration flag or traffic weight back to the last known-good version within minutes, without a full redeploy. Capture exemplar failing requests, diff template versions, and run offline replays against both versions to confirm the fix before attempting a forward roll again. Document the incident with the prompt version ids involved for postmortems." }
    ]),
  },
  {
    id: "aiml-mlsd-prompt-management-optimization",
    title: "Prompt Management & Optimization",
    description: "Optimize prompts systematically for cost, quality, and latency.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Wrench",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 13,
    content: emptyContent({ overview: "Optimize prompts systematically for cost, quality, and latency." }),
    article: toArticle([
      { type: 'paragraph', text: "Ad hoc tweaking in a playground does not scale. Mature teams treat prompts like hyperparameters: they define objectives (task success rate, constraint violations, latency), hold out evaluation sets with diverse edge cases, and change one axis at a time while tracking regressions. Systematic prompt engineering adds structure through rubrics, pairwise comparisons, and error clustering so improvements generalize beyond the examples that inspired a wording change. The goal is a repeatable workflow where every edit is tied to metrics and can be reverted without guesswork." },
      { type: 'paragraph', text: "Frameworks in the spirit of DSPy recast prompts as programs that compile into model-facing strings or demonstration sets given a training signal. Instead of manually rewriting instructions, you specify modules (signatures), provide a small set of labeled examples, and let an optimizer search over paraphrases, chain-of-thought scaffolding, or few-shot selections. This trades some upfront complexity for better coverage of the optimization landscape, especially when multiple components (retrieval, reasoning, formatting) must co-adapt. The approach still needs guardrails: overfitting to tiny dev sets is common if you do not maintain held-out tasks and periodic human review." },
      { type: 'paragraph', text: "Prompt compression targets redundant phrasing, duplicated policy text, and oversized few-shot blocks. Techniques include summarizing static policy into bullet constraints, moving reference material into retrieval rather than the system message, and using smaller models to rewrite verbose drafts under length caps. Each removal should be validated because aggressive shortening can drop implicit disambiguation that the model relied on, increasing format errors or safety failures even when headline accuracy looks fine on average." },
      { type: 'paragraph', text: "Evaluation-driven iteration closes the loop. Online signals (thumbs down, task completion, downstream parser success) feed dashboards; offline suites catch regressions before ship. The trade-off is measurement cost: rich human eval does not run on every commit, so you layer fast automated checks (schema validity, keyword constraints, embedding distance to reference answers) with slower periodic deep eval. The architecture should log prompts and outcomes at fine granularity so optimizers and analysts can attribute cost and quality changes to specific prompt versions." },
      { type: 'qa', question: "When would you prefer a DSPy-style optimizer over hand-written prompt iteration?", category: "Optimization", answer: "When you have enough labeled or scored examples to define a training objective, multiple composable modules whose prompts interact, and engineers who can invest in the optimizer setup and validation harness. Hand iteration stays better for tiny data, highly creative copy, or tasks where success is subjective and expensive to score automatically, provided you still track versions and run regression tests." },
      { type: 'qa', question: "How do you reduce LLM cost through prompt changes without wrecking quality?", category: "Cost", answer: "Profile token usage per stage and remove duplicate instructions, shrink few-shot sets using clustering or influence-style selection, push long reference text into retrieval, and prefer concise system messages tested on a benchmark. Pair every cut with offline eval and spot checks on long-tail intents, because the cheapest prompt is not cheaper overall if it raises failures, retries, or human escalations." },
      { type: 'qa', question: "What does an evaluation-driven iteration cadence look like in production?", category: "Evaluation", answer: "Every prompt change runs automated regression tests and schema checks in CI. Shadow or canary traffic compares new prompts against the incumbent on live-shaped traffic with guardrail metrics. Promote only if quality and safety metrics meet thresholds, then monitor drift over days because short canaries can miss weekly seasonality. Keep a feedback channel so failures cluster into new benchmark cases." }
    ]),
  },
  {
    id: "aiml-mlsd-chain-flow-orchestration",
    title: "Chain / Flow Orchestration",
    description: "Build multi-step LLM pipelines with LangGraph, DSPy, and custom DAGs.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Workflow",
    color: "bg-teal-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 14,
    content: emptyContent({ overview: "Build multi-step LLM pipelines with LangGraph, DSPy, and custom DAGs." }),
    article: toArticle([
      { type: 'paragraph', text: "Single-shot completion APIs are rarely enough for agents, retrieval-augmented generation, tool use, or human-in-the-loop approvals. Orchestration frameworks such as LangChain and LangGraph help you express a flow as nodes (LLM calls, retrievers, validators, tools) and edges (data dependencies and control flow). The important architectural choice is whether your workload is a directed acyclic graph of deterministic stages or a richer cyclic graph that models re-planning, loops until a quality check passes, or stateful agents that revisit earlier steps with updated context." },
      { type: 'paragraph', text: "DAG-style pipelines are easier to reason about: topological execution, clear batching points, and straightforward retries at stage boundaries. Graph or state-machine models add conditional branches (route by intent, model capability, or validation outcome), parallel fan-out for independent subtasks, and merge points where partial results must be reconciled. LangGraph-style approaches make the state object explicit so you can persist checkpoints, implement human interrupts, and resume after failures without replaying expensive upstream work." },
      { type: 'paragraph', text: "Error handling should be first-class. Transient provider errors warrant exponential backoff with jitter and idempotent tool calls where possible. Business-level failures (tool returned empty, parser rejected JSON) need compensating paths: alternate models, simplified prompts, or graceful degradation messages. Without this, chains become fragile cascades where one weak link aborts the user experience. Logging each node with inputs, outputs, latency, and token counts makes postmortems feasible when a branch misroutes traffic." },
      { type: 'paragraph', text: "Parallel execution improves latency when subtasks are independent, but you must manage token and rate limits across concurrent LLM calls and avoid race conditions on shared mutable state. State machines help agents by constraining which transitions are legal, embedding guardrails before dangerous tools, and making cyclic behavior (reflect, critique, revise) explicit rather than implicit recursion. The trade-off is engineering overhead: graph frameworks add learning curve and operational complexity compared to a simple script, which is acceptable when flows are long-lived, team-owned, and safety-critical." },
      { type: 'qa', question: "When would you choose a cyclic graph over a strict DAG for LLM orchestration?", category: "Architecture", answer: "Choose a cyclic graph when the task needs iteration until a verifier passes, dynamic replanning based on tool feedback, or conversational repair loops. A DAG fits predictable ETL-like flows with fixed stages. Cycles require careful termination conditions, cycle budgets, and state snapshots so you do not run unbounded loops or duplicate side effects on retries." },
      { type: 'qa', question: "How do you make multi-step chains reliable in production?", category: "Reliability", answer: "Use timeouts, retries only for idempotent or safely repeatable steps, structured error types, and per-node fallbacks. Persist checkpoints or minimal state needed to resume, validate outputs before downstream nodes consume them, and circuit-break hot failing tools or models. Emit traces so you can see which hop failed and whether user-visible partial results should be rolled back or annotated." },
      { type: 'qa', question: "How do you design conditional branching without unmaintainable nested logic?", category: "Design", answer: "Encode branch predicates as small pure functions over a typed state object, keep routing tables or policy objects separate from node implementations, and document invariants for each branch. Visual graph definitions or declarative configs help reviewers see all paths. Centralize side effects in named nodes so branches differ by data and control flow, not by copy-pasted LLM calls with tiny edits." }
    ]),
  },
  {
    id: "aiml-mlsd-structured-output-generation",
    title: "Structured Output Generation",
    description: "Enforce JSON schemas, function calling, and typed outputs from LLMs.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Code2",
    color: "bg-emerald-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 15,
    content: emptyContent({ overview: "Enforce JSON schemas, function calling, and typed outputs from LLMs." }),
    article: toArticle([
      { type: 'paragraph', text: "Downstream automation breaks when models emit prose instead of predictable records. Modern APIs expose JSON mode, constrained decoding, or function and tool calling interfaces that bias generation toward syntactically valid structures. Architecturally, you decide whether structure is enforced primarily by the model provider, by a grammar-constrained local decoder, or by post-hoc parsing and repair. Provider-native JSON modes reduce client complexity but tie you to vendor behavior and error messages; local constrained generation can add latency and integration work but offers portability." },
      { type: 'paragraph', text: "Application layers often model expected outputs with typed schemas (for example, Pydantic or JSON Schema) and validate parsed objects before business logic runs. Validation catches subtle issues: correct JSON with wrong field types, missing required keys, or enum values outside the allowed set. A common pattern is generate, parse, validate, and on failure issue a short repair prompt or switch to a smaller verifier model that only fixes syntax against the schema. Recursive summarization of validation errors into the retry prompt improves convergence compared to generic try again messages." },
      { type: 'paragraph', text: "Grammar-based generation and logit masking (where supported) reduce the probability of malformed tokens at the source by restricting each decoding step to tokens that keep the partial string in a valid grammar for JSON or a domain-specific language. This is powerful for strict protocols but can interact poorly with tokenizer boundaries and may not be available on every endpoint. Function calling treats the schema as a tool definition the model fills, which can yield higher reliability for tool-shaped tasks than free-form JSON instructions alone." },
      { type: 'paragraph', text: "Trade-offs include latency from retries, cost from multi-pass generation, and brittleness when the schema is overly rigid for genuinely fuzzy user intents. Good design matches schema granularity to the task, separates free-text user explanations from machine fields, and logs structured parse failures to refine prompts or schemas over time." },
      { type: 'qa', question: "Outline a robust retry flow when structured output fails validation.", category: "Implementation", answer: "Parse the raw completion, run schema validation, and if it fails return a compact error summary listing missing fields and type mismatches. Retry with the same model or a cheaper repair-focused prompt that sees only the invalid JSON and the schema excerpt, capped at a small number of attempts. If still failing, fall back to a deterministic template partial fill plus human review flag, and log the failure pattern for prompt or schema updates." },
      { type: 'qa', question: "Why might JSON mode still produce logically invalid outputs?", category: "Reliability", answer: "JSON mode generally enforces syntax, not semantics. The model can emit valid JSON with wrong keys, wrong types, hallucinated enum values, or values that violate cross-field constraints. Without a validation layer and business rules, consumers can misinterpret data silently. Always pair syntactic guarantees with schema validation and domain checks." },
      { type: 'qa', question: "How do you choose between function calling and a plain JSON schema prompt?", category: "Design", answer: "Prefer function calling when the provider implements it well for your target models and your output maps naturally to a tool invocation shape. Use explicit JSON schema prompts when you need broader portability, custom wrapping, or when the task is record extraction rather than tool routing. Base the decision on measured parse success, latency, and how tightly you need to couple arguments to named capabilities." }
    ]),
  },
  {
    id: "aiml-mlsd-guardrails-output-validation",
    title: "Guardrails & Output Validation",
    description: "Validate, filter, and constrain LLM outputs for safety and correctness.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "ShieldCheck",
    color: "bg-lime-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 16,
    content: emptyContent({ overview: "Validate, filter, and constrain LLM outputs for safety and correctness." }),
    article: toArticle([
      { type: 'paragraph', text: "Guardrails span the request path and the response path. Input guardrails screen for jailbreak patterns, prompt injection attempts, disallowed topics, oversized payloads, and sensitive attachments before they reach the model or tools. Output guardrails enforce tone policies, block prohibited content, redact or block personally identifiable information, and verify that claims stay within allowed knowledge boundaries when that matters for regulated domains. Architecturally, these are independent services or libraries with clear interfaces so policies can evolve faster than core model routing." },
      { type: 'paragraph', text: "Off-the-shelf frameworks such as NeMo Guardrails and Guardrails AI provide rule languages, validators, and integration patterns for chaining checks. They shine when you want declarative policies, conversation-level constraints (for example, stay on topic across turns), and reusable validators for structured fields. Custom validators remain essential for domain logic: verifying that a generated SQL references only allow-listed tables, that medical advice includes required disclaimers, or that JSON outputs satisfy business invariants. Combine lightweight regex and keyword filters for obvious cases with model-based classifiers for nuanced abuse, keeping latency budgets explicit." },
      { type: 'paragraph', text: "Fail-safe defaults matter as much as detection accuracy. When a classifier is uncertain, the safer product choice may be to refuse, ask a clarifying question, or route to human review rather than to stream a confident harmful answer. Logging blocked events with redacted excerpts supports tuning thresholds without storing excessive sensitive data. Be cautious of feedback loops where aggressive filtering degrades user trust; tune precision and recall against real traffic slices and measure false positives on legitimate edge cases." },
      { type: 'paragraph', text: "Trade-offs include latency stacking when many checks run sequentially, maintenance of policy rules across locales, and adversarial adaptation where attackers probe your stack. Parallelize independent checks, cache policy compilation results, and version policies alongside prompts so regressions are testable." },
      { type: 'qa', question: "How do you balance safety versus false positives on legitimate queries?", category: "Safety", answer: "Measure blocked rates and human review queues on stratified samples including sensitive but legitimate topics. Tune thresholds using precision-recall targets, add context-aware second-stage review for borderline scores, and expose user-visible explanations where appropriate. Continuously add false positives from user reports into regression tests so policy updates do not repeat the same mistakes." },
      { type: 'qa', question: "Where should guardrails live in a microservices LLM architecture?", category: "Architecture", answer: "Place coarse input filters at the edge or API gateway for rate and size limits, run policy-heavy checks in a dedicated policy service shared by all callers, and keep final output validation close to the channel that renders to users or triggers tools. Avoid duplicating contradictory rules in every service; centralize policy definitions with local enforcement hooks so latency and consistency remain manageable." },
      { type: 'qa', question: "How would you implement PII detection in an LLM response pipeline?", category: "Implementation", answer: "Run a detector ensemble: fast pattern-based scans for emails and phone-like strings, NER or transformer-based detectors for names and addresses, and optional allow-lists for known safe placeholders. On match, either redact with tokens, block the response, or route to human review based on severity. Log counts and categories without retaining raw PII unless legally authorized, and test with multilingual and formatted edge cases." }
    ]),
  },
  {
    id: "aiml-mlsd-conversation-short-term-memory",
    title: "Conversation / Short-Term Memory",
    description: "Manage conversation context windows and sliding-window strategies.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "MessageSquare",
    color: "bg-amber-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 17,
    content: emptyContent({ overview: "Manage conversation context windows and sliding-window strategies." }),
    article: toArticle([
      { type: 'paragraph', text: "Short-term memory in LLM applications is whatever you place in the model context for the current request: system instructions, tool definitions, retrieved documents, prior user and assistant turns, and hidden scratchpad content. The hard constraint is the context window measured in tokens, not characters, which interacts with model pricing, latency, and quality because very long contexts can dilute attention to early or buried details. Architecture starts with an explicit token budget per category so no single source silently crowds out others." },
      { type: 'paragraph', text: "Sliding windows keep only the most recent k turns verbatim, which is simple and predictable but drops early user constraints such as preferences or task definitions. Alternatives include summarizing older turns into a rolling digest, retaining the first n turns plus a summary of the middle, or importance-weighted retention where heuristics or a small model score which messages to keep. System prompt optimization matters because static instructions repeat every call; compress policies into minimal bullets and move rarely used reference material to retrieval on demand." },
      { type: 'paragraph', text: "Message pruning must preserve tool-call and tool-result pairs where the API requires strict ordering, and should not break structured conversations where the assistant asked a clarifying question the user answered ten turns ago. Some stacks inject periodic reminders of immutable facts extracted at session start (session variables) so they survive pruning better than raw chat text." },
      { type: 'paragraph', text: "Trade-offs are sharp: aggressive pruning saves cost and latency but increases forgetfulness and contradiction risk; always carrying full history avoids that until you hit limits or confuse the model with noise. Measure retention policies against task completion metrics and user frustration signals, not token savings alone." },
      { type: 'qa', question: "How would you design a token budget across system, tools, RAG, and history?", category: "Architecture", answer: "Allocate fixed caps per section with overflow handling: reserve headroom for the model completion, cap retrieved chunks by relevance and diversity, bound tool schemas, and give history a dynamic slice after fixed costs. When retrieval or history exceeds its cap, degrade gracefully by trimming lowest-scored chunks or oldest turns while preserving pinned facts and open tool threads. Emit metrics when trims occur so you can tune budgets from data." },
      { type: 'qa', question: "What performance implications does long conversation context have?", category: "Performance", answer: "Longer prompts increase prefill compute and time-to-first-token linearly or superlinearly depending on implementation, and raise per-request dollar cost. They can also slow end-to-end latency if you serialize large payloads to the provider. Caching of prefixes helps when system and document prefixes repeat. Pruning reduces all of these but shifts work to summarization passes if you use them." },
      { type: 'qa', question: "Compare sliding-window history with summarization-based retention.", category: "Trade-offs", answer: "Sliding windows are cheap, deterministic, and easy to debug but forget early constraints. Summarization preserves gist at the cost of an extra model call, possible hallucination of facts in the summary, and engineering complexity to refresh summaries safely. Hybrid approaches often keep the first segment verbatim, summarize the middle, and keep recent turns raw for fidelity on active subtasks." }
    ]),
  },
  {
    id: "aiml-mlsd-long-term-vector-memory",
    title: "Long-Term Vector Memory",
    description: "Store and retrieve episodic memory using vector databases.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "HardDrive",
    color: "bg-orange-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 18,
    content: emptyContent({ overview: "Store and retrieve episodic memory using vector databases." }),
    article: toArticle([
      { type: 'paragraph', text: "Long-term vector memory stores text or multimodal segments as dense vectors so semantically similar queries retrieve relevant past content, documentation, or user-specific notes. Managed services such as Pinecone, open-source engines like Qdrant and Weaviate, or Postgres with pgvector each differ in operational model, filtering expressiveness, replication story, and cost curve. Architecture choices include whether vectors live beside operational OLTP data, in a dedicated search tier, or replicated for regional latency." },
      { type: 'paragraph', text: "Indexing strategies dominate recall-latency trade-offs. HNSW graphs offer strong query performance at the cost of higher memory footprint and sometimes slower index builds. IVF variants partition the space into clusters to reduce search scope, which helps very large corpora when parameters are tuned to your data distribution. Metadata filtering (tenant id, document acl, time range, language) should be pushed into the index where supported so you do not over-fetch vectors that fail security or freshness rules after the fact." },
      { type: 'paragraph', text: "Hybrid search combines dense similarity with lexical signals such as BM25, which helps on rare strings, SKUs, and exact identifiers that embeddings blur. Common patterns include running parallel dense and sparse retrievals then fusing scores, or using lexical prefiltering to shrink the candidate set. Memory retrieval for agents often adds reranking with a cross-encoder or a small LLM to reorder the top k before injection into the prompt, trading compute for precision." },
      { type: 'paragraph', text: "Operational concerns include embedding model versioning (re-embed when the encoder changes), staleness and deletion semantics, and monitoring hit quality with human labels or proxy metrics. Without those, vector memory becomes a slow drift problem where retrieved context is plausible but wrong." },
      { type: 'qa', question: "How would you architect multi-tenant vector storage with access control?", category: "Architecture", answer: "Partition logically by tenant with mandatory metadata filters on every query, enforce ACLs at ingest so vectors inherit document-level permissions, and avoid cross-tenant shared namespaces without strong namespacing keys. Prefer databases that combine vector search with relational filters atomically. Audit query paths to ensure user-supplied filters cannot widen scope, and replicate indexes per region if latency requirements demand it." },
      { type: 'qa', question: "When would you pick HNSW versus IVF for a billion-scale corpus?", category: "Performance", answer: "HNSW is attractive when you need low latency queries and can afford RAM for graph structures, and when incremental inserts are frequent. IVF shines when you can afford offline clustering and want to bound search to a small number of partitions, especially on hardware where memory is tighter. The decision should be validated with representative QPS, recall at k targets, and build-time constraints on your pipeline." },
      { type: 'qa', question: "Describe a production retrieval pattern from query to ranked chunks in the prompt.", category: "Implementation", answer: "Embed the query with the same model version used at ingest, apply metadata filters for tenant and freshness, retrieve a larger candidate set with ANN search, then rerank top candidates with a cross-encoder or lightweight model. Deduplicate near-duplicate chunks, respect a token budget when stitching snippets, and log retrieval ids with the final answer for debugging and feedback loops." }
    ]),
  },
  {
    id: "aiml-mlsd-semantic-caching",
    title: "Semantic Caching",
    description: "Cache semantically similar queries to reduce latency and cost.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Thermometer",
    color: "bg-rose-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 19,
    content: emptyContent({ overview: "Cache semantically similar queries to reduce latency and cost." }),
    article: toArticle([
      { type: 'paragraph', text: "Exact-match caches miss when users rephrase the same intent. Semantic caching keys requests by an embedding of normalized text (and optionally structured context such as locale or model parameters), then searches a store of past (query embedding, response, metadata) pairs for neighbors above a similarity threshold. On a hit, you can return the cached completion immediately or use it as a draft for a cheaper verification pass. The architecture parallels recommendation nearest-neighbor retrieval but optimizes for equivalence of task outcomes rather than personalization." },
      { type: 'paragraph', text: "Similarity thresholds encode a policy choice: higher thresholds reduce false reuse where wording differences actually change the answer, while lower thresholds increase hit rate but risk serving incorrect or noncompliant text for subtly different constraints. Multi-field keys help, for example hashing tool definitions and schema versions into the cache key so a prompt or capability change does not silently reuse incompatible answers. GPTCache and similar libraries illustrate embedding backends, vector stores, and eviction policies you can adapt to your stack." },
      { type: 'paragraph', text: "Invalidation is harder than for byte-identical caches. You need rules for TTL on time-sensitive topics, explicit busting when underlying documents or tools change version, and per-tenant purge APIs for privacy requests. For regulated answers, prefer shorter TTLs or disable semantic reuse entirely. Monitoring should track hit rate, estimated dollar savings, and downstream error rates attributed to cache hits versus misses." },
      { type: 'paragraph', text: "Trade-offs include storage growth, embedding compute on every request, and the cold-start period before the cache is warm. False positives are more damaging than a cache miss, so conservative thresholds and optional lightweight judges often pay for themselves." },
      { type: 'qa', question: "How would you tune semantic cache similarity thresholds in production?", category: "Performance", answer: "Start conservative with high similarity and measure hit rate, user corrections, and task failure rates on cached responses versus misses. Use shadow mode that logs would-be hits without serving them to estimate savings safely. Adjust by segment because creative writing tolerates looser matching than legal or medical style tasks. Revisit thresholds when you change embedding models because geometry shifts." },
      { type: 'qa', question: "How do you estimate cost savings from semantic caching?", category: "Cost", answer: "Compare token usage and model tier costs for served traffic with a counterfactual without cache using logged prompt lengths. Include embedding costs and vector store queries in the net savings. Report savings net of any verifier model calls you add. Watch for traffic shaping effects where faster responses increase total usage, partially offsetting per-request savings." },
      { type: 'qa', question: "What invalidation strategies prevent stale cached answers?", category: "Architecture", answer: "Combine TTLs with version stamps for knowledge bases, tools, and prompts in the cache key, and provide admin APIs to purge by tenant or tag. For retrieval-heavy answers, invalidate when source document versions change. For high-risk domains, shorten TTL or require periodic revalidation against a checksum of retrieved evidence ids stored with the cache entry." }
    ]),
  },
  {
    id: "aiml-mlsd-memory-summarization-compression",
    title: "Memory Summarization & Compression",
    description: "Compress conversation history while preserving key information.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Archive",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 20,
    content: emptyContent({ overview: "Compress conversation history while preserving key information." }),
    article: toArticle([
      { type: 'paragraph', text: "As sessions lengthen, verbatim history stops scaling. Summarization pipelines condense older content into shorter narratives or bullet memories while preserving decision-relevant details. Progressive summarization updates a running summary each turn or every n turns, amortizing cost and keeping latency predictable compared with recomputing from full history. Hierarchical memory stores summaries at multiple timescales (recent turns raw, session summary, user profile facts) so the model can attend to the right granularity for the current subtask." },
      { type: 'paragraph', text: "Stronger compression goes beyond prose: entity extraction builds a typed graph of people, accounts, and preferences; fact tables store constraints the user stated explicitly; and task state machines record open goals. Recursive summarization chains repeatedly compress summaries for very long threads, which is powerful but accumulates abstraction error if each step drops nuance. Mitigations include retaining citations to original turn ids, storing confidence flags, and running periodic consistency checks that compare extracted facts against the latest user messages." },
      { type: 'paragraph', text: "Information loss is the central trade-off. Summaries hallucinate or merge incompatible constraints; aggressive compression improves token metrics but can remove negation, numbers, or legal qualifiers that change meaning. Product and safety reviews should define which fields must never be summarized away (for example, dosage numbers, account ids) and keep those in structured slots rather than narrative text." },
      { type: 'paragraph', text: "Implementation-wise, choose summarizer models smaller than the main reasoning model when possible, validate outputs with schema where applicable, and log diff metrics when replacing raw history with summaries so regressions are visible in offline replay." },
      { type: 'qa', question: "How would you design hierarchical memory for a long-running support agent?", category: "Architecture", answer: "Keep the last few exchanges verbatim for tone and pending questions, maintain a rolling session summary with open tickets and decisions, and persist a durable user profile of stable preferences and account facts validated against tools. Route retrieval so the model sees session summary plus relevant profile slices rather than an undifferentiated blob. Version each layer so debugging can trace what the model saw." },
      { type: 'qa', question: "What are the main risks of recursive summarization chains?", category: "Trade-offs", answer: "Each hop can drop details, blur temporal order, or introduce confident-sounding inaccuracies. Errors compound because later stages only see compressed text. Mitigate with structured extraction for critical facts, caps on recursion depth, occasional full-context refresh for high-stakes threads, and human escalation paths when confidence is low." },
      { type: 'qa', question: "How would you implement fact compression alongside narrative summaries?", category: "Implementation", answer: "Run a structured extraction pass that outputs JSON facts with provenance turn ids, merge into a canonical store with conflict resolution rules, and inject a concise fact list into the system context while the narrative summary handles story flow. Validate types and enums in code, and reconcile updates when new messages contradict old facts rather than appending duplicates blindly." }
    ]),
  },
  {
    id: "aiml-mlsd-state-persistence-retrieval",
    title: "State Persistence & Retrieval",
    description: "Persist agent and workflow state across sessions and restarts.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Save",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 21,
    content: emptyContent({ overview: "Persist agent and workflow state across sessions and restarts." }),
    article: toArticle([
      { type: 'paragraph', text: "Stateful agents and multi-step workflows need more than an in-memory variable between HTTP requests. Persistence stores conversation transcripts, partial plan objects, tool outputs awaiting approval, and cursor positions in batch jobs. Checkpointing captures a serializable snapshot at stable boundaries: after each tool call, after human confirmation, or on a timer for streaming tasks. Without clear boundaries, you either over-persist noisy intermediate states or under-persist and lose hours of work on crash." },
      { type: 'paragraph', text: "Serialization should be explicit about schema versioning and backward compatibility. Prefer JSON with typed records or protobuf over pickled Python objects, and avoid embedding non-portable handles unless you also persist what they reference. Large binary artifacts belong in object storage with references in the state document. Encrypt sensitive fields at rest and scope encryption keys per tenant where compliance requires it." },
      { type: 'paragraph', text: "Session management ties user identity, device, and workflow id to a state record in Redis for low-latency ephemeral coordination or in a relational database when you need transactions, auditing, and complex queries. Redis suits short-lived locks, rate limits, and fast resume; Postgres suits authoritative records and compliance trails. Hybrid designs use Redis as a hot cache with asynchronous write-through to durable storage." },
      { type: 'paragraph', text: "Distributed coordination arises when multiple workers handle one session or when you scale orchestration horizontally. You need optimistic concurrency with version counters, leases with TTL to avoid stuck locks, and idempotent side effects so retries after partial failure do not double-charge or double-send. Recovery patterns include replay from last checkpoint, compensating transactions for irreversible steps, and dead-letter queues for sessions that cannot proceed." },
      { type: 'qa', question: "How do you recover a long agent run after a worker crash?", category: "Reliability", answer: "Reload the last successful checkpoint from durable storage, validate schema version and referenced artifacts still exist, and resume from the next node in the graph. Re-execute idempotent steps freely; gate irreversible external calls behind idempotency keys and compare stored intent hashes before retrying. Surface partial results to the user with a clear resumed status and audit log of the failure window." },
      { type: 'qa', question: "When would you choose Redis versus a SQL database for workflow state?", category: "Architecture", answer: "Use Redis when you need sub-millisecond reads, ephemeral coordination, and can tolerate optional durability trade-offs unless you enable persistence features carefully. Use SQL when you need strong durability guarantees, complex querying, relational integrity across tables, and straightforward backup and compliance reporting. Many systems use Redis for fast locks plus async persistence to SQL for source of truth." },
      { type: 'qa', question: "What serialization pitfalls break agent checkpointing in production?", category: "Implementation", answer: "Non-serializable objects such as open sockets or closures, unbounded growth of debug fields, implicit timezone-naive timestamps, and breaking changes to field names without migrations. Also watch for secrets ending up in plaintext snapshots. Mitigate with explicit DTOs, size limits, migration shims, and field-level encryption for credentials or tokens referenced by state." }
    ]),
  },
  {
    id: "aiml-mlsd-tracing-logging",
    title: "Tracing & Logging",
    description: "Capture end-to-end traces of prompts, responses, tool calls, and retrievals.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Eye",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 22,
    content: emptyContent({ overview: "Capture end-to-end traces of prompts, responses, tool calls, and retrievals." }),
    article: toArticle([
      { type: 'paragraph', text: "Large language model applications behave like distributed systems even when they run in a single service: a request may fan out to embeddings, vector search, rerankers, tool executors, and multiple model calls. OpenTelemetry gives you vendor-neutral spans with parent-child relationships, baggage for tenant or experiment identifiers, and exporters that land in Jaeger, Honeycomb, Grafana Tempo, or cloud APM. For LLMs specifically, you instrument not only HTTP latency but semantic phases such as prompt templating, tokenizer calls, retrieval, and post-processing so on-call engineers can see where time and tokens accumulate rather than staring at one opaque wall clock." },
      { type: 'paragraph', text: "Logging prompts and completions is necessary for debugging and audit, but it is also a privacy and compliance minefield. Structured logs with redaction pipelines, field-level encryption, configurable sampling by risk tier, and retention policies aligned with legal holds reduce exposure compared to dumping raw transcripts into plaintext files. Tool call traces should capture arguments at a safe granularity, return payloads or hashes depending on sensitivity, and correlate with upstream span IDs so you can replay failures without losing causality. Retrieval traces benefit from logging query text or a stable hash, index version, top-k identifiers, and scores so you can distinguish ranking regressions from model regressions." },
      { type: 'paragraph', text: "Platforms such as Langfuse, LangSmith, and Arize Phoenix specialize in LLM observability: they combine traces with cost, latency, and evaluation scores, and often support dataset export for fine-tuning or regression suites. The trade-off is another data plane to secure and another bill line item versus building on generic tracing alone. Generic tracing is flexible and integrates with existing SRE dashboards; LLM-native products accelerate teams that want prompt versioning, annotation workflows, and scorecards without wiring everything by hand." },
      { type: 'paragraph', text: "The operational sweet spot is usually hybrid: OpenTelemetry for service-to-service propagation and SLO dashboards, plus selective enrichment spans or linked events for model-specific context, and a curated observability store for high-value sampled conversations. That keeps cardinality manageable while preserving enough signal to debug subtle failures like stale retrieval caches or non-deterministic tool ordering." },
      { type: 'qa', question: "How would you instrument an LLM agent that calls external tools and a vector database while keeping traces useful for on-call?", category: "Observability", answer: "Create a root span per user request and child spans for each phase: retrieval, each tool invocation with sanitized arguments, the main completion call, and post-processing. Propagate trace context through async workers and queues, attach stable attributes such as model name, prompt template version, and index snapshot id, and cap payload sizes in span events while linking to secure blob storage for full payloads when needed. Use consistent span names and status codes so dashboards can aggregate error rates by component." },
      { type: 'qa', question: "What architectural decisions matter when logging prompts and model outputs in a regulated environment?", category: "Architecture", answer: "Separate control plane from data plane logging, default to deny for full transcript storage, classify routes by data sensitivity, apply tokenization or hashing for high-risk fields, encrypt at rest with customer-managed keys where required, and tie retention to purpose such as debugging versus training. Provide user-facing consent flows if logs feed product improvement, and ensure access is role-based with break-glass procedures rather than broad shared admin accounts." },
      { type: 'qa', question: "When would you adopt Langfuse, LangSmith, or Arize versus only generic OpenTelemetry backends?", category: "Tooling", answer: "Adopt an LLM-native platform when teams need prompt experiment tracking, human labels tied to traces, built-in cost and latency breakdown per model, and faster time-to-value than custom dashboards. Stay on generic backends when you have strict data residency constraints, already standardized on a single APM vendor, or need minimal third-party data movement and are willing to build evaluation and prompt analytics in-house." }
    ]),
  },
  {
    id: "aiml-mlsd-latency-token-monitoring",
    title: "Latency & Token Monitoring",
    description: "Track inference latency percentiles and token consumption patterns.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Timer",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 23,
    content: emptyContent({ overview: "Track inference latency percentiles and token consumption patterns." }),
    article: toArticle([
      { type: 'paragraph', text: "Latency for generative models is not a single number. Clients perceive time-to-first-token, or TTFT, as responsiveness, while total completion time grows with output length. Server-side metrics therefore need both TTFT and tokens per second after the first chunk, plus end-to-end request duration including prefill, batching waits, and streaming overhead. Histograms that export P50, P95, and P99 let you catch tail behavior where a minority of requests hit cold caches, oversized prompts, or overloaded GPUs; averages alone hide saturation." },
      { type: 'paragraph', text: "Token usage is both a cost driver and a proxy for work done. Tracking input versus output tokens per route, model, and tenant supports chargeback and capacity planning. Queue depth at schedulers, batch formation layers, or Kubernetes pod autoscaler signals impending backlog before P99 explodes. Pair queue metrics with admission control or shedding policies so alerts lead to actionable playbooks rather than panic after user-visible timeouts." },
      { type: 'paragraph', text: "SLOs should be defined on user-visible slices: for chat, TTFT under a budget for interactive tiers and streaming chunk cadence within bounds; for batch jobs, job completion time and backlog age. SLAs are contractual and often stricter; leave margin between internal SLO targets and external commitments. Optimizing TTFT typically involves KV-cache reuse, smaller effective prefill through prompt compression, speculative decoding where applicable, and right-sizing max concurrency so prefill is not starved by decode-heavy workloads." },
      { type: 'paragraph', text: "Trade-offs appear everywhere: aggressive batching improves throughput and cost per token but hurts tail latency for small requests. Dynamic batching with timeout caps balances the two but complicates autoscaling signals. Token-based autoscaling is more faithful than CPU for GPU workloads but reacts more slowly; combining predictive scaling with queue-depth alerts reduces oscillation." },
      { type: 'qa', question: "Why are P95 and P99 often more important than average latency for LLM APIs?", category: "Performance", answer: "Averages are dominated by short requests and hide long tails where users abandon flows or triggers cascade retries. P95 and P99 expose contention, cold starts, oversized contexts, and scheduler starvation. SRE practices use percentiles to set budgets and error budgets that match real user pain, especially for streaming interfaces where a stalled tail feels like a hung application." },
      { type: 'qa', question: "How would you define an SLO for a streaming chat product versus a batch summarization pipeline?", category: "SLOs", answer: "For chat, define SLOs on TTFT below a threshold for a high fraction of sessions, plus a maximum gap between streamed chunks so the UI never appears frozen. For batch jobs, target queue time plus wall clock to completion per job size class, and measure backlog age so delayed processing is visible before deadlines slip. Keep measurement windows and exclusions for canary traffic explicit so error budgets stay meaningful." },
      { type: 'qa', question: "What metrics beyond wall-clock latency help you diagnose a sudden regression in LLM serving?", category: "Metrics", answer: "Inspect batch size distributions, prefill versus decode time splits, GPU memory pressure, KV-cache hit rates if applicable, tokenizer overhead, retrieval latency if RAG is in path, rate-limit or retry storms from upstream dependencies, and per-model token throughput. Compare input token histograms for context bloat and check deployment timestamps against configuration changes such as max sequence length or quantization mode." }
    ]),
  },
  {
    id: "aiml-mlsd-cost-tracking-attribution",
    title: "Cost Tracking & Attribution",
    description: "Attribute LLM costs to features, users, and teams for budgeting.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "DollarSign",
    color: "bg-teal-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 24,
    content: emptyContent({ overview: "Attribute LLM costs to features, users, and teams for budgeting." }),
    article: toArticle([
      { type: 'paragraph', text: "LLM bills are volatile: list prices change, cached tokens discount prefill, and tool calls add hidden compute. Production systems should compute an estimated cost per request at inference time using model id, pricing table version, input and output token counts, and any surcharges such as embedding or reranker calls. Persisting cost alongside trace identifiers lets finance and engineering reconcile provider invoices with internal usage and spot anomalies like runaway loops or prompt injection that balloon tokens." },
      { type: 'paragraph', text: "Attribution ties those costs to business dimensions: feature flags, product surfaces, customer tiers, engineering teams, or experiments. Tag requests at the edge with stable dimensions propagated through async work so allocation survives fan-out. Without tags, centralized AI platforms become opaque cost centers. With tags, product leaders can compare unit economics across flows and decide where cheaper models or caching are acceptable." },
      { type: 'paragraph', text: "Budget alerts should be layered: soft warnings when burn rate exceeds forecast, harder throttles for runaway jobs, and executive dashboards for monthly variance. Optimization strategies include prompt compression, retrieval limits, model routing to smaller models for easy subtasks, response length caps, semantic caching for repeated queries, and batch APIs for non-interactive work. Chargeback moves real dollars to cost centers and drives behavior fast but adds accounting overhead and can discourage shared platform investment. Showback displays costs without internal billing and is gentler for adoption but weaker for accountability." },
      { type: 'paragraph', text: "Governance ties cost to policy: approved models per data class, approval for high-spend experiments, and guardrails so junior services cannot call frontier models by default. Architecturally, a central inference gateway can enforce metering, quotas, and logging in one place rather than reimplementing limits in every client." },
      { type: 'qa', question: "How do you attribute shared infrastructure cost when many microservices call the same LLM gateway?", category: "Cost", answer: "Require every caller to send non-optional dimensions such as service name, product id, and environment, validate them at the gateway, and aggregate usage in a warehouse keyed by those dimensions. For untagged legacy traffic, fall back to inferred attribution from API keys or mTLS identity and run remediation programs. Amortize reserved GPU or committed spend across tenants using agreed rules such as proportional token use or fixed platform fees plus variable usage." },
      { type: 'qa', question: "What are the trade-offs between chargeback and showback for an internal AI platform?", category: "Governance", answer: "Chargeback creates strong incentives to optimize and fund shared capacity fairly but increases disputes, forecasting work, and risk that teams bypass the platform to avoid internal bills. Showback educates owners with lower friction and keeps collaboration high but may not change behavior until budgets are exceeded externally. Hybrid models with showback first and selective chargeback for high-spend teams are common during platform maturation." },
      { type: 'qa', question: "Where in the architecture should cost metering live for LLM calls?", category: "Architecture", answer: "A centralized inference proxy or sidecar is ideal because it sees final token counts, model routing decisions, and retries in one place and can apply consistent pricing versions. Client-only estimates drift when retries and streaming chunk accounting differ from provider logs. The gateway should emit immutable metering events to an analytics pipeline while still allowing async reconciliation against vendor invoices nightly." }
    ]),
  },
  {
    id: "aiml-mlsd-quality-metrics",
    title: "Quality Metrics",
    description: "Measure faithfulness, relevance, helpfulness, and coherence of outputs.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Star",
    color: "bg-emerald-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 25,
    content: emptyContent({ overview: "Measure faithfulness, relevance, helpfulness, and coherence of outputs." }),
    article: toArticle([
      { type: 'paragraph', text: "Quality for generative systems is multi-dimensional. Faithfulness measures whether claims are supported by provided context, which matters acutely for retrieval-augmented generation where models should not invent sources. Relevance asks whether the answer addresses the user intent, including follow-up disambiguation when the query is underspecified. Helpfulness and coherence capture polish: actionable steps, consistent tone, and non-contradictory reasoning. No single scalar replaces human judgment, but a balanced scorecard prevents teams from optimizing one metric while silently degrading others." },
      { type: 'paragraph', text: "Frameworks like RAGAS formalize reference-based and reference-free scores using smaller models or LLM judges: for example, context precision and recall for retrieval, answer groundedness against contexts, and answer relevance to the question. Automated scoring scales regression testing and release gates but inherits judge bias and can overfit to the evaluator's quirks. Ground-truth evaluation with curated labeled sets remains the anchor for calibration: you measure agreement between automated metrics and human ratings and track drift over time." },
      { type: 'paragraph', text: "Human-AI agreement rates quantify how often expert annotators align with model-assigned labels or scores. Low agreement signals ambiguous rubrics, underspecified tasks, or evaluator fatigue. Improving agreement usually means clearer guidelines, edge-case exemplars, adjudication for borderline items, and periodic rater calibration sessions rather than endlessly tuning the automated metric alone." },
      { type: 'paragraph', text: "Trade-offs include cost and latency of running evaluators on every production response versus sampling; reference-free metrics enable breadth but are noisier; reference-based metrics need maintained datasets that go stale as products evolve. The pragmatic path combines a frozen golden set for strict regression, periodic refresh of adversarial or user-mined examples, and lightweight online signals such as thumbs or implicit dwell time for triage." },
      { type: 'qa', question: "How does RAGAS-style evaluation differ from a single end-to-end accuracy number on a Q&A benchmark?", category: "Evaluation", answer: "RAGAS-style metrics decompose the pipeline into retrieval quality and generation quality so you can localize failures, whereas a single accuracy collapses retrieval misses, grounding errors, and formatting issues into one opaque score. Decomposed metrics guide interventions such as better chunking versus prompt changes. They still require careful interpretation because automated subscores can correlate and because reference-free judges introduce their own variance." },
      { type: 'qa', question: "What should you monitor alongside automated quality scores in production?", category: "Metrics", answer: "Monitor sampling rate, judge model version, score distributions for sudden shifts, correlation with human spot checks, and segment breakdowns by language or domain. Track input length and retrieval hit rate because metric changes may reflect cohort shifts rather than true quality moves. Alert on discontinuities after prompt or retriever deploys and on divergence between automated scores and human feedback aggregates." },
      { type: 'qa', question: "How do you implement a regression gate using automated quality metrics without blocking good releases?", category: "Implementation", answer: "Maintain versioned golden sets with statistical thresholds rather than pointwise perfection, run metrics in CI with fixed judge seeds where applicable, and use two-tier gates: hard fail on large drops on core scenarios, soft warnings on secondary suites. Allow overrides with documented risk acceptance for time-sensitive fixes, and always pair metrics with a small human review sample for high-risk domains like medical or legal assistance." }
    ]),
  },
  {
    id: "aiml-mlsd-hallucination-safety-detection",
    title: "Hallucination & Safety Detection",
    description: "Detect and flag hallucinated content and unsafe outputs in real time.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "AlertTriangle",
    color: "bg-lime-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 26,
    content: emptyContent({ overview: "Detect and flag hallucinated content and unsafe outputs in real time." }),
    article: toArticle([
      { type: 'paragraph', text: "Hallucinations range from subtle factual slips to confident fabrication of citations and APIs. Detection strategies include natural language inference models that check whether each sentence is entailed by retrieved context, self-consistency checks that compare multiple sampled answers for stability, and citation verification pipelines that resolve claimed URLs or document ids back to the corpus. None are foolproof: NLI models have their own errors, self-consistency is expensive, and citation strings can be well-formed but wrong." },
      { type: 'paragraph', text: "Safety classifiers address policy violations: hate, harassment, sexual content, self-harm instructions, malware assistance, and PII leakage. They may run on user input, model output, or both, with tiered models from fast linear baselines to heavier transformers or frontier models for escalation. Real-time filtering trades latency for coverage; asynchronous review can catch slower abuse patterns such as coordinated probing. Confidence scoring is fragile when models are miscalibrated, but combining verbalized uncertainty with consistency checks and retrieval score thresholds still helps triage responses for human review or templated safe fallbacks." },
      { type: 'paragraph', text: "Grounding techniques reduce hallucination upstream: force answers to quote spans, require structured outputs that reference passage ids, use constrained decoding when schemas are known, and refuse when retrieval confidence is low. Architecturally, detection often lives in a policy layer wrapping the model service so policies can evolve without retraining the base model. Logging blocked attempts supports adversarial analysis and threshold tuning." },
      { type: 'paragraph', text: "The central trade-off is false positives versus false negatives. Aggressive blocking protects brand and compliance but angers legitimate users; permissive settings increase incident risk. Calibrate thresholds per surface, offer appeals or human escalation paths for high-stakes flows, and measure downstream metrics such as override rates and time-to-resolution rather than classifier accuracy alone." },
      { type: 'qa', question: "Describe how you would combine retrieval grounding with an NLI-based entailment check for factual QA.", category: "Safety", answer: "Retrieve top passages with scores, generate an answer conditioned only on those passages, then split the answer into atomic claims and run an NLI model pairwise against each supporting passage or the union of passages. Flag or rewrite claims that are contradicted or neutral with weak evidence. Add a final policy pass for unsafe content even when claims are entailed, because grounded harmful instructions are still unacceptable." },
      { type: 'qa', question: "What are limitations of self-consistency for hallucination detection?", category: "Detection", answer: "Self-consistency assumes that the majority of diverse samples cluster around a correct answer, which fails under systematic biases, narrow sampling, or when all samples share a mistaken premise from bad retrieval. It multiplies inference cost and latency. It also struggles with subjective or creative tasks where multiple valid answers exist, so the method fits structured factual settings better than open-ended brainstorming." },
      { type: 'qa', question: "Where should safety and hallucination checks live in a microservices architecture for LLMs?", category: "Architecture", answer: "Place fast input filters at the edge or API gateway to drop obvious abuse before expensive work, run generation in a dedicated model service, then apply output classifiers and grounding verifiers in a policy microservice that can be updated independently. Stream partial tokens only after policies that can operate on prefixes if needed, or buffer minimally when checks require full answers. Centralize audit logs and configuration for thresholds to avoid inconsistent enforcement across clients." }
    ]),
  },
  {
    id: "aiml-mlsd-llm-as-judge-evaluation",
    title: "LLM-as-Judge Evaluation",
    description: "Use LLMs to evaluate other LLM outputs with rubric-based scoring.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Gavel",
    color: "bg-amber-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 27,
    content: emptyContent({ overview: "Use LLMs to evaluate other LLM outputs with rubric-based scoring." }),
    article: toArticle([
      { type: 'paragraph', text: "Using a language model to score or rank other model outputs scales evaluation beyond human panels, but only if the judging protocol is disciplined. Rubrics should define discrete score anchors with concrete behaviors, include negative examples, and separate dimensions such as correctness, verbosity, and tone so judges do not conflate them. Pairwise comparison, where the judge picks A or B with optional ties, often yields more stable preferences than absolute grading because relative judgments reduce scale drift." },
      { type: 'paragraph', text: "Reference-free evaluation asks the judge only for the prompt and candidate answer, sometimes plus context for RAG. It is portable when gold answers are unavailable but risks circularity if judge and generator share similar biases or training data. Reference-based evaluation anchors to ground truth when it exists and is usually preferred for factual tasks, though constructing references is expensive. Judge model selection is a trade-off: stronger judges correlate better with humans but cost more; weaker judges add noise and can be swayed by position bias or eloquent wrong answers." },
      { type: 'paragraph', text: "Bias in LLM judges is well documented: position bias favors the first or second answer depending on model and framing; verbosity bias rewards longer responses; self-enhancement bias favors outputs from the same family as the judge. Mitigations include swapping answer order across duplicate trials, using blinded labels, calibrating scores with a small human-labeled set, and aggregating across multiple judges or prompts. Multi-judge consensus reduces variance at the cost of compute; disagreement itself is a useful signal for ambiguous items worth human review." },
      { type: 'paragraph', text: "Calibration means mapping raw judge scores to probabilities or human-equivalent scales so thresholds for release gates are interpretable. Without calibration, a score of four out of five is not comparable across judge versions or prompt templates." },
      { type: 'qa', question: "Why is pairwise judging often preferred to absolute 1-5 scores when using an LLM as a judge?", category: "Evaluation", answer: "Relative comparisons reduce calibration drift because the judge anchors on two concrete options in the same context rather than mapping to an abstract numeric scale that shifts across sessions. Pairwise designs also map naturally to preference learning for reward models. The downside is more comparisons needed to rank many candidates and potential intransitivity if not aggregated carefully." },
      { type: 'qa', question: "How would you design a methodology to detect and reduce position bias in LLM judges?", category: "Methodology", answer: "Run each pair twice with swapped order and discard or downweight inconsistent decisions, report agreement across orders as a quality metric for the judge setup, and use neutral framing that does not hint which system is baseline. Augment with a subset of human-adjudicated pairs to measure bias magnitude and track it over judge model upgrades. Consider Bradley-Terry or Elo aggregation instead of raw win counts when comparing many systems." },
      { type: 'qa', question: "What are the main trade-offs of multi-judge consensus versus a single strong judge model?", category: "Trade-offs", answer: "Multiple judges reduce variance and catch idiosyncratic failures but increase latency and cost linearly unless run in parallel with batching. A single strong judge is simpler operationally but concentrates model-specific biases. A hybrid is common: a cheap judge for triage and a strong judge for borderline cases or high-risk domains, with human review when judges disagree." }
    ]),
  },
  {
    id: "aiml-mlsd-alerting-dashboards",
    title: "Alerting & Dashboards",
    description: "Build operational dashboards with anomaly detection and alerting.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "LayoutDashboard",
    color: "bg-orange-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 28,
    content: emptyContent({ overview: "Build operational dashboards with anomaly detection and alerting." }),
    article: toArticle([
      { type: 'paragraph', text: "Dashboards for LLM systems should tell a story in three layers: user experience, pipeline health, and economics. The experience layer shows TTFT, error rates, and client-side abandonment if instrumented. The pipeline layer breaks out retrieval, reranking, model inference, and tool calls with saturation indicators such as queue depth and thread pool usage. The economics layer tracks tokens, cost per successful request, and cache hit rates so incidents that spare latency still surface when they burn budget." },
      { type: 'paragraph', text: "Visualization choices matter: heatmaps by model version and route expose canary regressions; burn-rate charts for SLOs highlight fast versus slow violations of error budgets. Anomaly detection on seasonal metrics such as weekend traffic can reduce false positives compared to static thresholds, but needs baselines that account for marketing launches and model upgrades. SLO burn-rate alerting, popularized by Google SRE practice, pages engineers when consumption of the monthly error budget accelerates dangerously even if instantaneous rates look fine." },
      { type: 'paragraph', text: "Integrations with Grafana, Datadog, or cloud-native observability stacks reuse existing on-call rotations, escalation policies, and incident timelines. LLM-specific nuance is that some failures are soft quality degradations without HTTP 500s, so alerts should include evaluation score drops, rising toxicity block rates, or sudden embedding latency spikes. Runbooks should link from alerts to dashboards filtered to the failing region and to recent deploys or feature flags." },
      { type: 'paragraph', text: "Trade-offs include alert fatigue from flaky judges or sampled metrics versus missing silent failures when sampling is too sparse. Align alert thresholds with customer-visible pain, use multi-window burn rates, and route non-urgent quality drift to daytime tickets rather than pages." },
      { type: 'qa', question: "What distinguishes a good on-call runbook for an LLM serving incident from a generic API outage runbook?", category: "Operations", answer: "It includes steps for checking model version rollouts, tokenizer or template mismatches, GPU OOM patterns, retrieval index freshness, rate limits from upstream providers, and quality metric dashboards not just status codes. It names owners for prompt changes versus infra changes, documents safe rollback including prior model weights or cached prompt ids, and lists communication templates for customer-facing degradation when outputs are low quality but technically 200 OK." },
      { type: 'qa', question: "How would you architect dashboards so product and platform teams share one source of truth?", category: "Architecture", answer: "Store canonical metrics in a time-series backend with consistent naming conventions and required dimensions such as product, model, and region. Build curated boards per persona that read from the same metric definitions rather than duplicating queries. Use recording rules or metric layers so complex formulas live in one place, version dashboard JSON in git where possible, and document semantic meaning of each panel to avoid divergent interpretations." },
      { type: 'qa', question: "When would you use multi-window burn-rate alerts instead of simple threshold alerts on error rate?", category: "Monitoring", answer: "Multi-window burn rates catch both sharp spikes and gradual leaks of reliability by comparing short and long windows against SLO budgets. Simple thresholds miss slow burns that exhaust the budget over days or misfire on noisy low-traffic routes. Burn-rate alerts align paging to customer impact tied to explicit SLOs, though they require careful tuning of severity windows and minimum event counts to avoid flapping on sparse data." }
    ]),
  },
  {
    id: "aiml-mlsd-offline-evaluation-benchmarks",
    title: "Offline Evaluation Benchmarks",
    description: "Design benchmark suites for systematic model comparison before deployment.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "FlaskConical",
    color: "bg-rose-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 29,
    content: emptyContent({ overview: "Design benchmark suites for systematic model comparison before deployment." }),
    article: toArticle([
      { type: 'paragraph', text: "Offline benchmarks are the controlled laboratory of LLM engineering. A serious suite combines broad capability checks with domain-specific scenarios drawn from real failures, edge cases for tool use and structured output, multilingual slices, and safety probes. Each case should carry metadata: difficulty, expected tools, required citations, or numeric tolerances for regression triage. Golden datasets curated by experts anchor automated metrics, but they rot as the product and user language evolve, so pipelines to mine new negatives from production with human approval loops are essential." },
      { type: 'paragraph', text: "Automated scoring spans exact match, fuzzy metrics, execution-based checks for code, LLM judges with fixed rubrics, and deterministic validators such as JSON schema conformance. Multi-dimensional evaluation prevents shipping a model that improves summarization while silently breaking JSON mode. Version every asset: prompts, judge prompts, and scoring code live in git with the same discipline as application logic." },
      { type: 'paragraph', text: "Test set contamination undermines leaderboard optimism and internal confidence. Hold out truly private sets, deduplicate near-duplicates against training corpora where possible, and segregate sets used for development tuning from those reserved for final gatekeeping. Leakage from web crawl overlap is hard to eliminate entirely; mitigations include dynamic evaluation refreshed from licensed or internal corpora and canary user sessions excluded from training." },
      { type: 'paragraph', text: "Statistical significance matters when differences are small. Use confidence intervals for win rates across paired comparisons, account for multiple testing when scanning many metrics, and prefer stratified reporting so aggregate lifts do not hide harm to tail demographics. The trade-off is evaluation runtime versus power: larger suites reduce variance but slow CI, so tiered suites with a fast smoke set and a nightly comprehensive job are standard." },
      { type: 'qa', question: "How would you structure a benchmark suite so it catches both capability regressions and safety regressions?", category: "Methodology", answer: "Partition suites by risk and intent: core functional cases with deterministic checks, RAG grounding cases with retrieval fixtures, safety red-team prompts with policy expectations, and performance microbenchmarks for latency-sensitive templates. Run safety and high-risk suites on every release candidate even if smaller, and keep capability breadth in scheduled jobs if full runs are too slow for every commit." },
      { type: 'qa', question: "What design choices reduce the risk of benchmark overfitting during iterative model development?", category: "Design", answer: "Maintain a frozen holdout set never used for hyperparameter or prompt iteration, rotate in fresh adversarial examples from production sampling, track performance on external public benchmarks only as a secondary signal, and monitor correlation between offline scores and online outcomes. Discourage cherry-picking cases that always pass and document any changes to the suite with reviewer sign-off." },
      { type: 'qa', question: "How do you implement automated scoring in CI without making pipelines flaky or prohibitively slow?", category: "Implementation", answer: "Cache deterministic fixtures, parallelize shards across workers, pin judge models and seeds, retry only on known transient provider faults with caps, and separate quick gating jobs from exhaustive nightly jobs. Store artifacts such as diffs and score JSON for debugging. Fail builds on statistically meaningful drops using precomputed baselines rather than single-point noise." }
    ]),
  },
  {
    id: "aiml-mlsd-online-production-evaluation",
    title: "Online / Production Evaluation",
    description: "Evaluate model quality in production with shadow scoring and live metrics.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Radio",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 30,
    content: emptyContent({ overview: "Evaluate model quality in production with shadow scoring and live metrics." }),
    article: toArticle([
      { type: 'paragraph', text: "Offline benchmarks miss real user diversity, messy inputs, and interaction effects with live retrieval indexes. Online evaluation closes the loop by measuring behavior under production traffic, always subject to ethical constraints and privacy review. Shadow evaluation routes duplicate requests to a candidate model without showing outputs to users, preserving safety while collecting latency and automatic scores on mirrored inputs. It stresses capacity because you pay twice for shadowed traffic, so sampling and selective shadowing on low-risk cohorts are common mitigations." },
      { type: 'paragraph', text: "Interleaving and A/B tests expose users to variants with explicit assignment. Interleaving interleaves results from two rankers or answer generators in controlled ways for preference inference in search; for chat, paired designs are more typical than strict interleaving. Live quality scoring runs lightweight judges or classifiers on a sample of production transcripts, sometimes asynchronously to avoid adding tail latency. User satisfaction proxies include explicit thumbs, task completion downstream, edit distance when users revise outputs, and session continuation rates, each noisy but complementary." },
      { type: 'paragraph', text: "Production sampling must balance coverage with cost and privacy: stratify by language, paying customers, new features, and error codes; oversample failures; and cap retention. Real-time quality dashboards combine operational metrics with sampled judge scores and human review throughput so teams detect slow drifts such as index staleness or subtle prompt regressions." },
      { type: 'paragraph', text: "Trade-offs include experimentation risk to user trust, regulatory limits on using chats for evaluation without consent, and statistical challenges when metrics are highly skewed. Strong governance, clear opt-in for training versus evaluation use, and coordination with offline suites reduce blind spots." },
      { type: 'qa', question: "When is shadow evaluation preferable to a user-visible A/B test for a new LLM version?", category: "Architecture", answer: "Shadow is better when the candidate is immature, safety properties are unproven, or product requirements forbid changing user-visible outputs. It measures infrastructure compatibility, latency, and side-by-side scores without UX risk. It cannot measure true adoption, satisfaction, or downstream business metrics that require exposure, so plan a path to limited A/B after shadow confidence is high." },
      { type: 'qa', question: "How would you design an interleaving or paired comparison experiment for two answer generators in a support copilot?", category: "Methodology", answer: "Randomize eligible sessions to receive either system A or B with stable assignment per ticket to avoid mixed experiences, or present blinded side-by-side drafts only to agents if policy allows, collecting preference clicks. Predefine primary metrics such as resolution time and agent preference, guardrail metrics for policy violations, and minimum sample sizes. Control for ticket difficulty with stratification or post-hoc regression on metadata such as product area." },
      { type: 'qa', question: "What operational concerns matter most when running live LLM judges on production traffic?", category: "Operations", answer: "Cost and latency budgets, PII redaction before judging, judge model availability and version drift, sampling bias if only English is judged, alert fatigue from judge failures, and clear data retention policies. Provide kill switches, rate limits, and backpressure so judge outages never take down the primary serving path. Audit disagreement between judges and human spot checks regularly." }
    ]),
  },
  {
    id: "aiml-mlsd-automated-regression-testing",
    title: "Automated Regression Testing",
    description: "Catch quality regressions with automated test suites on every change.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Bug",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 31,
    content: emptyContent({ overview: "Catch quality regressions with automated test suites on every change." }),
    article: toArticle([
      { type: 'paragraph', text: "Traditional software regression tests assert deterministic outputs. LLM outputs are stochastic, so automated regression shifts toward property-based checks, structured output validation, semantic similarity bands, and golden snapshots with tolerances. A practical suite combines frozen prompts with recorded fixtures for retrieval, executes tools in sandboxes when behavior must be exact, and uses small deterministic models for cheap smoke tests while reserving frontier models for nightly gates." },
      { type: 'paragraph', text: "Snapshot testing for prompts captures expected completions under pinned model versions and decoding parameters. When snapshots change, reviewers inspect diffs for intentional improvements versus regressions. This pattern mirrors Jest snapshots but requires discipline: noisy snapshots that change every provider maintenance create churn, so prefer assertions on extracted JSON fields or intermediate representations when possible." },
      { type: 'paragraph', text: "CI integration treats evaluation jobs as first-class workflows with artifacts, flaky-test quarantine policies, and parallelism across shards. Assertion-based evaluation includes schema validators, regex for required disclaimers, unit tests on post-processing code, and binary checks that forbidden substrings never appear on safety cases. Flaky tests arise from temperature greater than zero, provider nondeterminism, and race conditions in parallel tool calls; mitigations are temperature zero for CI, retries only where documented, and marking known provider issues." },
      { type: 'paragraph', text: "Coverage strategies span intent classes, languages, token lengths, and failure modes from past incidents. Coverage is never complete; the goal is representative stress plus continuous growth from production failures. Reliability also means fast feedback: tiered pipelines keep merge requests snappy while protecting main from silent degradation." },
      { type: 'qa', question: "How do you test LLM applications in CI when outputs are not bitwise reproducible?", category: "Testing", answer: "Pin models and decoding settings for smoke tiers, assert on structured fields and tool call arguments, use multiple samples with majority vote only in nightly jobs if needed, and compare embeddings or ROUGE-style metrics within thresholds for free text when appropriate. Separate deterministic unit tests for all non-model code paths so flakiness is isolated to explicitly labeled suites." },
      { type: 'qa', question: "What belongs in a CI/CD pipeline versus a scheduled offline job for LLM regression?", category: "CI/CD", answer: "CI should run fast schema and policy checks, small golden prompt sets with cheap models, static analysis on prompts and tools, and contract tests for APIs. Scheduled jobs run larger judge-based suites, multilingual corpora, and load tests that would slow every pull request. Promote failures from scheduled jobs to new CI cases when they represent repeatable high-severity defects." },
      { type: 'qa', question: "How do you handle a flaky evaluation test that sometimes fails without a clear product regression?", category: "Reliability", answer: "Reproduce with fixed seeds and logged contexts, check for provider capacity or version skew, tighten assertions to less brittle properties, move borderline cases to human-reviewed sets, and quarantine only with an owner and expiry. Track flakiness rates in metrics; chronic flakes erode trust and hide real failures. Prefer root-cause fixes over blind retries except for documented transient network errors with tight limits." }
    ]),
  },
  {
    id: "aiml-mlsd-human-feedback-collection",
    title: "Human Feedback Collection",
    description: "Design feedback loops for collecting and incorporating human judgments.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "ThumbsUp",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 32,
    content: emptyContent({ overview: "Design feedback loops for collecting and incorporating human judgments." }),
    article: toArticle([
      { type: 'paragraph', text: "Human feedback remains the gold standard for subtle quality, safety, and brand alignment. The feedback UI should minimize cognitive load: clear instructions, keyboard shortcuts, side-by-side layouts for preferences, and visible timers or quotas to prevent rater burnout. For free-text critique, constrain dimensions with rubrics and exemplars so comments are actionable for model or prompt updates rather than vague dissatisfaction." },
      { type: 'paragraph', text: "Annotation tools need workflow features: queue management, adjudication for disagreements, versioning of tasks when prompts change, and audit trails for compliance. Inter-annotator agreement metrics such as Cohen kappa or Fleiss kappa quantify rubric clarity; low agreement usually indicates ambiguous guidelines or underspecified edge cases before it indicates lazy raters. Aggregation rules might be majority vote, adjudicator override, or Bayesian models that weight rater reliability learned from gold items sprinkled into production batches." },
      { type: 'paragraph', text: "Active learning prioritizes labeling examples where the model is uncertain, diverse in embedding space, or likely failure modes from error detection. It improves data efficiency but can skew the dataset toward hard negatives if not balanced with random sampling to preserve calibration. Preference collection at scale pairs responses from two policies and records which humans prefer, feeding reward modeling or direct preference optimization pipelines." },
      { type: 'paragraph', text: "Trade-offs include cost, latency of human loops versus shipping velocity, and selection bias when only power users leave feedback. Instrument silent signals and stratified sampling to avoid optimizing only for vocal minorities." },
      { type: 'qa', question: "What makes a rubric effective for collecting comparable human judgments on LLM outputs?", category: "Design", answer: "It defines each score level with concrete behaviors, includes anchor examples for borderline cases, separates dimensions such as factuality and tone, and explains common failure modes specific to your product. Iteratively update the rubric when adjudication patterns show recurring confusion, and train raters with calibration tasks before production labeling." },
      { type: 'qa', question: "How would you measure and improve inter-annotator agreement on a subjective helpfulness task?", category: "Methodology", answer: "Start with pilot batches, compute kappa or rank correlation, run adjudication sessions to refine definitions, and add negative and positive control items with known labels to detect drift. Track per-rater statistics privately for coaching, not punishment without context, and rotate raters across items to separate item difficulty from rater bias." },
      { type: 'qa', question: "How do you scale preference collection without drowning in redundant near-duplicate examples?", category: "Scaling", answer: "Use embedding deduplication and clustering to diversify batches, cap contributions per user session to reduce spam, apply active learning to focus on model disagreement regions, and subsample high-volume happy paths. Store raw preferences with metadata for later reweighting, and periodically rebalance toward underrepresented languages or domains." }
    ]),
  },
  {
    id: "aiml-mlsd-ab-testing-prompts-models",
    title: "A/B Testing for Prompts & Models",
    description: "Run controlled experiments to compare prompts, models, and configurations.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Split",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 33,
    content: emptyContent({ overview: "Run controlled experiments to compare prompts, models, and configurations." }),
    article: toArticle([
      { type: 'paragraph', text: "Prompt and model changes are not traditional UI tweaks; they alter distributions of tokens, safety outcomes, and downstream automation. Experiments need preregistered hypotheses, primary metrics such as task success or revenue proxies, and guardrail metrics including toxicity rate, P95 latency, cost per session, and escalation to human support. Traffic splitting should be stable per user or session to avoid inconsistent experiences, implemented at a reliable assignment layer with salted hashes rather than ad hoc random calls in each microservice." },
      { type: 'paragraph', text: "Statistical power depends on baseline rates, minimum detectable effect, and variance. Rare events like safety violations need enormous samples, so composite guardrails and Bayesian monitoring help, but do not replace careful power analysis before launch. Sequential testing methods allow earlier stopping under controlled false-positive rates when effects are large, which suits fast iteration environments, though they require statistical literacy to avoid peeking abuse." },
      { type: 'paragraph', text: "Multi-armed bandits allocate more traffic to better-performing arms dynamically, useful when exploring many prompt variants or retrieval configurations. They trade exploration for exploitation: pure bandits optimize short-term reward but can under-explore risky tails, so constrained bandits with safety caps or periodic exploration phases are safer for LLM products." },
      { type: 'paragraph', text: "Architecture considerations include feature flags with consistent bucketing, back-end enforcement so clients cannot self-select variants, and idempotent logging of assignments for analysis. When models differ in cost, cap concurrent exposure of expensive arms and monitor infrastructure saturation as a first-class outcome." },
      { type: 'qa', question: "How do you choose metrics and guardrails for an A/B test comparing two system prompts for a customer-facing assistant?", category: "Experimentation", answer: "Pick a primary metric aligned with the product goal such as successful task completion or customer satisfaction proxy, and define guardrails on latency, cost per conversation, harmful output rate, human handoff rate, and regression on critical intents from a labeled mini-eval. Precommit to stopping rules and duration, and segment readouts by cohorts like language or plan tier to catch hidden harms." },
      { type: 'qa', question: "What statistical pitfalls appear when analyzing LLM experiments with high variance outcomes?", category: "Statistics", answer: "High variance widens confidence intervals and demands larger samples; peeking without sequential correction inflates false positives; multiple metrics increase family-wise error unless adjusted; Simpson's paradox can appear if traffic mix shifts during the test. Use preanalysis plans, cluster standard errors if users contribute repeated sessions, and validate with offline held-out sets when online signals are noisy." },
      { type: 'qa', question: "When would you use a multi-armed bandit instead of a fixed 50-50 A/B allocation for prompt selection?", category: "Architecture", answer: "Use bandits when you have many candidates, exploration cost is tolerable, and the reward signal is frequent enough for learning within the experiment window, such as click-through on suggested replies. Prefer fixed A/B for rare or high-risk outcomes, when you need clean causal readouts for executive reporting, or when infrastructure cannot handle uneven unpredictable load from adaptive routing without autoscaling headroom." }
    ]),
  },
  {
    id: "aiml-mlsd-drift-detection",
    title: "Drift Detection",
    description: "Monitor for data, concept, and output drift in production systems.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "TrendingDown",
    color: "bg-teal-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 34,
    content: emptyContent({ overview: "Monitor for data, concept, and output drift in production systems." }),
    article: toArticle([
      { type: 'paragraph', text: "Drift means the world changed faster than your training assumptions. Covariate shift appears when input distributions move: new slang, new document types in RAG corpora, or a marketing campaign that sends different user demographics. Concept drift is when the relationship between inputs and desired outputs changes even if inputs look similar, such as updated policies or seasonal intent. Output drift tracks changes in model predictions or scores over time and can reflect either upstream input shifts or internal regressions after deploys." },
      { type: 'paragraph', text: "Monitoring often starts with univariate statistical tests on engineered features, but text-heavy systems lean on distribution summaries such as vocabulary novelty rates, language mix, average token length, and embedding centroid shifts. Population stability index compares binned score distributions between a reference window and a current window; high PSI suggests redistribution of model confidence or inputs, though PSI is sensitive to binning choices and should be interpreted alongside business context." },
      { type: 'paragraph', text: "Embedding drift compares sample embeddings from production to a reference set using distances in vector space, nearest-neighbor overlap, or clustering stability. Alerts need thresholds tuned to avoid noise: use adaptive baselines, require persistence across multiple windows, and correlate with quality or error metrics before auto-remediation. Remediation strategies include refreshing training data, re-indexing retrieval corpora, recalibrating classifiers, rolling back prompts or models, and increasing human review rates for affected segments." },
      { type: 'paragraph', text: "LLM-specific drift includes retrieval corpus staleness, tool API behavior changes, and judge model upgrades that move evaluation scores without changing the product model. Tag incidents by suspected drift type so playbooks stay actionable." },
      { type: 'qa', question: "How would you distinguish data drift from a bad model deployment using only monitoring signals?", category: "Monitoring", answer: "Correlate timelines: a deploy spike in errors with stable input statistics points to the release, while gradual embedding drift with rising perplexity-like proxies and stable code suggests upstream population change. Compare canary slices, check versioned artifacts such as index build ids, and if shadow traffic on the prior model worsens similarly, the environment drifted; if only the new model worsens, suspect the change itself." },
      { type: 'qa', question: "What are strengths and weaknesses of PSI for monitoring LLM scoring or retrieval ranker outputs?", category: "Detection", answer: "PSI is simple to operationalize and interpret as a coarse redistribution alarm when score distributions shift between reference and current periods. Weaknesses include sensitivity to binning, lack of localization for which subpopulations moved, and possible false alarms when benign seasonality repeats. For text, PSI on scalar proxies is indirect; pair it with embedding-based tests and qualitative sampling." },
      { type: 'qa', question: "What operational steps follow a sustained high-severity drift alert in production?", category: "Operations", answer: "Page or ticket the owning team with linked dashboards, increase sampling for human review on affected segments, consider temporary traffic throttling or fallback to a safer model, open a incident record with hypothesized causes, and schedule remediation such as retraining, corpus refresh, or hotfix prompts. Communicate to stakeholders if user-visible quality changed, and after resolution add a new regression case to offline suites to catch recurrence." }
    ]),
  },
  {
    id: "aiml-mlsd-cicd-ct-pipelines",
    title: "CI/CD/CT for Prompts & Pipelines",
    description: "Automate testing, validation, and deployment of prompts and ML pipelines.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Workflow",
    color: "bg-emerald-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 35,
    content: emptyContent({ overview: "Automate testing, validation, and deployment of prompts and ML pipelines." }),
    article: toArticle([
      { type: 'paragraph', text: "Modern ML systems ship three moving parts in lockstep: application code, training and inference pipelines, and the artifacts they consume (datasets, prompts, model weights). Continuous integration for ML extends beyond unit tests to include data schema checks, statistical tests on slices, lightweight model smoke tests, and contract tests between training jobs and serving containers. Pipelines should pin dependencies with lockfiles, build reproducible training images, and fail fast when a pull request breaks backward compatibility for feature stores or serialization formats." },
      { type: 'paragraph', text: "Continuous training (CT) complements CI/CD by deciding when to retrain or refresh models based on schedules, drift monitors, or business events rather than only on merges to main. Architecturally, CT is a workflow orchestrator (Airflow, Prefect, Argo Workflows, GitHub Actions on cron) that kicks off training with immutable inputs: a git commit hash, a dataset snapshot id, and a configuration bundle. The same CI gates that protect application deploys should gate model promotion: offline metrics thresholds, fairness checks on held-out demographics, latency and memory budgets on representative hardware, and canary analysis hooks before traffic shifts." },
      { type: 'paragraph', text: "Prompt and LLM pipeline CI deserves the same rigor. Treat prompts and tool definitions as versioned assets: run static analysis for injection-prone concatenation, render templates against golden fixtures, snapshot outputs on a frozen model stub or a small representative model tier, and diff token counts against budgets. For RAG, validate that retrieval connectors return structured documents within latency SLOs and that chunking logic is deterministic across environments. GitHub Actions and GitLab CI are natural homes for these steps because they integrate with branch protection, required reviews, and secret management for API keys used in evaluation jobs." },
      { type: 'paragraph', text: "Trade-offs center on cost versus coverage. Full fine-tune or large-model evals on every commit are expensive; teams often tier checks (lint and schema on every push, nightly regression suites, manual promotion for major releases). Another tension is speed of iteration versus blast radius: permissive pipelines let researchers move fast but increase the odds of shipping incompatible tokenizer bundles or silently shifted data distributions. The pragmatic pattern is artifact immutability, explicit promotion stages, and making every production inference traceable to a pipeline run id." },
      { type: 'qa', question: "How would you layer CI checks for an LLM feature that uses RAG and tool calling?", category: "Automation", answer: "I would run fast checks on every pull request: template schema validation, static checks for unsafe string assembly, unit tests for parsers and tool routers, and mocked retrieval with fixed fixtures to assert prompt assembly. Nightly or merge-to-main jobs would hit real vector indexes with read-only credentials, run regression prompts against pinned model versions, and capture latency histograms. Promotion to production would require a green bundle that includes evaluation summaries, cost estimates from token usage, and signed artifacts for prompts and configuration hashes tied to the git commit." },
      { type: 'qa', question: "Where should continuous training live relative to application CI/CD?", category: "Architecture", answer: "CT should be a separate orchestrated workflow with its own schedules and triggers, but it should reuse the same build artifacts and validation gates as application CD so training containers and serving containers stay aligned. Events from drift detection or performance monitors publish to a queue or webhook that starts a training job with immutable dataset references. The output model version flows through a registry and only reaches inference after the same promotion pipeline used for code, keeping a single source of truth for what is allowed in production." },
      { type: 'qa', question: "What reliability practices matter most for ML pipelines in CI?", category: "Reliability", answer: "Pin data snapshots and random seeds where feasible, cache intermediate artifacts with content-addressable storage, and make failures actionable with structured logs that include run ids and input hashes. Use idempotent training steps and automatic retries only for transient infrastructure faults, not for nondeterministic divergence. Enforce timeouts and resource limits so a stuck GPU job does not block the merge queue, and surface flaky tests with quarantine policies rather than silent reruns that hide regressions." }
    ]),
  },
  {
    id: "aiml-mlsd-experiment-tracking-version-control",
    title: "Experiment Tracking & Version Control",
    description: "Track experiments, hyperparameters, and artifacts systematically.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "GitCommitHorizontal",
    color: "bg-lime-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 36,
    content: emptyContent({ overview: "Track experiments, hyperparameters, and artifacts systematically." }),
    article: toArticle([
      { type: 'paragraph', text: "Experiment tracking answers a deceptively simple question: what exactly did we run, and how did it compare to everything else? Platforms such as MLflow Tracking and Weights and Biases centralize scalar metrics, parameter dictionaries, tags, source code versions, environment fingerprints, and pointers to large artifacts like checkpoints or evaluation reports. Without that spine, notebooks and ad hoc scripts become the system of record, and reproducing a leaderboard result weeks later is often impossible." },
      { type: 'paragraph', text: "Hyperparameter logging is only the surface layer. Mature setups log dataset identifiers, preprocessing commit hashes, tokenizer versions, evaluation harness versions, and hardware details (GPU model, driver, batch size) because small shifts in any of these can move metrics more than a learning-rate tweak. Artifact versioning ties trained weights to the exact code and data that produced them; object stores or managed artifact backends keep blobs out of git while still preserving lineage through content hashes and signed metadata." },
      { type: 'paragraph', text: "Reproducibility is a spectrum. Full bitwise reproducibility across GPUs is rare, but engineering teams can target statistical reproducibility within tight confidence intervals by fixing seeds where libraries allow, recording nondeterminism flags, and storing container images or conda lockfiles. Comparison dashboards then become decision tools: filter runs by task, data snapshot, or author, diff hyperparameter grids, and overlay learning curves to catch early divergence that predicts a bad allocation of compute budget." },
      { type: 'paragraph', text: "The main trade-off is observability overhead versus researcher velocity. Heavy mandatory fields slow exploration; too light a schema invites junk tags that do not support audits. A balanced approach defines a minimal required schema per project (task id, data version, primary metric) and lets optional rich metadata attach as JSON. Another tension is cost of long-term storage for large checkpoints versus the need to resume training; retention policies should align with regulatory needs and model deprecation schedules." },
      { type: 'qa', question: "How do MLflow and Weights and Biases differ in typical adoption patterns?", category: "Tooling", answer: "MLflow is often chosen when teams want an open-source stack they can self-host, tight integration with Spark and batch training jobs, and a model registry in the same ecosystem. Weights and Biases emphasizes collaborative experiment UI, rich media logging for gradients and media, and managed SaaS with low operational burden. Both support REST APIs and Python SDKs; the choice frequently hinges on hosting constraints, budget for SaaS, and whether the organization already standardized on one vendor for the rest of MLOps." },
      { type: 'qa', question: "What would you log to maximize reproducibility of a fine-tuning run?", category: "Reproducibility", answer: "I would log the git commit, full dependency lockfile or container digest, base model identifier and revision, dataset snapshot ids with checksums, train and validation splits construction code, tokenizer and vocabulary hash, training configuration including optimizer state format, mixed precision settings, and random seed policy. I would store evaluation scripts and benchmark versions, plus artifacts for checkpoints at intervals. For distributed training, I would log world size, framework version, and any custom kernels so another engineer can replay or at least closely approximate the run." },
      { type: 'qa', question: "How should teams structure experiment naming and tagging methodology?", category: "Methodology", answer: "Use stable project namespaces and human-readable run names that encode the hypothesis, not only the date. Tags should separate concerns: environment (staging versus research), dataset slice, model family, and milestone such as paper submission or production candidate. Avoid overloading a single tag field with composite strings that are hard to query. Establish code review norms for experiments that declare production intent so metadata is complete before sharing results with stakeholders or promoting a model." }
    ]),
  },
  {
    id: "aiml-mlsd-model-registry-governance",
    title: "Model Registry & Governance",
    description: "Catalog, version, and govern models from training to retirement.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Archive",
    color: "bg-amber-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 37,
    content: emptyContent({ overview: "Catalog, version, and govern models from training to retirement." }),
    article: toArticle([
      { type: 'paragraph', text: "A model registry is the operational catalog between training output and inference deployment. It stores versioned model packages with metadata: metrics on canonical benchmarks, intended use constraints, training data provenance, fairness evaluations, and owning teams. Consumers resolve a logical model name to a pinned stage such as staging or production, which decouples frequent training iterations from the stability requirements of live traffic. Governance layers add approval workflows so risk, legal, or domain experts sign off before promotion." },
      { type: 'paragraph', text: "Model cards and documentation are not paperwork for its own sake; they encode the assumptions under which metrics were measured and the known failure modes. Approval workflows should attach evidence: links to experiment runs, evaluation notebooks or reports, and change summaries when architecture or data shifts. Staging environments mirror production feature pipelines so promoted models are validated against realistic inputs before receiving customer load. Deprecation policies specify end-of-life dates, fallback models, and communication paths so dependent services do not silently reference retired versions." },
      { type: 'paragraph', text: "Lineage tracking connects datasets, feature engineering jobs, training code revisions, and deployed endpoints. When an upstream data vendor corrects historical records or a bug is found in a labeler tool, lineage makes blast-radius analysis feasible: which models consumed affected partitions and which endpoints must be rolled back or retrained. Governance policies translate organizational risk appetite into automated checks: block promotion if mandatory tests are missing, if PII scanning failed, or if a high-risk use case lacks secondary review." },
      { type: 'paragraph', text: "Trade-offs include centralization versus team autonomy. A strict global registry improves auditability but can bottleneck teams if every hotfix needs a committee. Federated registries per business unit with shared interfaces sometimes scale better. Another tension is metadata completeness versus form fatigue; dynamic forms that adapt to model type (tabular versus generative) keep required fields relevant without drowning computer vision teams in NLP-specific questions." },
      { type: 'qa', question: "What gates would you require before promoting a generative model to production?", category: "Governance", answer: "I would require documented intended use and out-of-scope uses, safety and content policy evaluations including adversarial prompts, privacy checks for memorization or PII leakage on representative corpora, latency and cost envelopes, and rollback plans. Technical gates include reproducible evaluation artifacts, versioned training data references, and sign-off from product and policy owners. Automated checks would enforce presence of those artifacts and block if red-team findings are open without explicit exception records." },
      { type: 'qa', question: "How does a model registry fit into a microservices architecture?", category: "Architecture", answer: "Serving services resolve model ids and stages from the registry API or synced configuration rather than baking file paths into containers. Training pipelines publish new versions to the registry after validation, and deployment controllers pull immutable bundles from object storage using registry-supplied URIs and checksums. This separation lets inference scale independently, supports blue-green and canary releases, and keeps authorization for who can promote models centralized at the registry boundary." },
      { type: 'qa', question: "How would you operationalize model deprecation?", category: "Operations", answer: "I would announce deprecation with a timeline, mark the registry stage as deprecated with warnings in client SDKs, and monitor remaining traffic by consumer id. Automated jobs would migrate workloads to the successor model with shadow comparison where needed, then enforce hard cutoffs after the window. I would retain artifacts per retention policy for audits, archive evaluation reports, and run post-deprecation reviews to capture lessons for the next lifecycle." }
    ]),
  },
  {
    id: "aiml-mlsd-automated-retraining-finetuning",
    title: "Automated Retraining & Fine-tuning",
    description: "Trigger retraining and fine-tuning based on data drift or schedule.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "RotateCw",
    color: "bg-orange-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 38,
    content: emptyContent({ overview: "Trigger retraining and fine-tuning based on data drift or schedule." }),
    article: toArticle([
      { type: 'paragraph', text: "Automated retraining starts with explicit trigger semantics. Schedule-based triggers are predictable and good for domains with seasonal patterns or stable label refresh cadences. Performance-based triggers fire when online metrics cross thresholds compared to a baseline window, which ties retraining to user-visible regressions but requires careful guardrails against noisy short-term spikes. Drift triggers compare input distributions, label priors, or embedding geometry between reference and recent traffic; they catch silent upstream pipeline changes even when accuracy has not yet moved." },
      { type: 'paragraph', text: "Orchestration ties those signals to idempotent training pipelines. A typical flow validates incoming data volume and schema, snapshots training inputs, launches a job with resource quotas, writes metrics and artifacts to experiment tracking, and opens a pull request or registry candidate rather than pushing straight to production. Data freshness constraints matter: stale features or lagging labels can make a technically successful training run worse than the incumbent. Fine-tuning large language models often uses parameter-efficient methods such as LoRA, which injects low-rank adapters into attention projections so most base weights stay frozen, or QLoRA, which quantizes the base model in low-bit formats while keeping adapter updates in higher precision to reduce VRAM and speed iteration." },
      { type: 'paragraph', text: "Validation gates before promotion should mirror production evaluation harnesses: held-out sets, business KPI proxies, safety suites, and latency measurements on target hardware. For adapters, verify compatibility with the exact base model revision and tokenizer bundle served in inference. Canary deployments compare error rates and tail latency against the champion model on a small traffic slice before full rollout." },
      { type: 'paragraph', text: "Trade-offs include trigger sensitivity versus alert fatigue. Over-triggered pipelines burn GPU budget and fragment the model lineage; under-triggered systems let slow rot accumulate until incidents force emergency retrains. LoRA and QLoRA reduce cost but may underfit when the task requires full-model capacity or deep adaptation; full fine-tunes are more expensive and risk catastrophic forgetting without careful regularization and replay strategies." },
      { type: 'qa', question: "How would you combine drift detection with performance triggers?", category: "Automation", answer: "I would treat them as complementary signals in a rules engine. Drift without metric drop might only raise a ticket or schedule a low-priority retrain, while sustained metric regression triggers an urgent pipeline with tighter approval. I would require minimum sample sizes and time windows before acting to reduce false positives, and I would log every trigger decision with the statistics that caused it for postmortems. Human override should remain possible when domain knowledge explains benign drift." },
      { type: 'qa', question: "When would you choose LoRA versus full fine-tuning for production LLM customization?", category: "Architecture", answer: "LoRA fits when the base model already encodes broad competence and you need style, format, or narrow domain adaptation with limited labeled data and tight GPU budgets. Full fine-tuning is more appropriate when the domain vocabulary or reasoning patterns diverge strongly from pretraining, or when adapter capacity limits quality after iterative evaluation. Architecture-wise, serving must load base weights once and hot-swap or compose adapters per tenant where product requirements demand isolation without duplicating full models." },
      { type: 'qa', question: "What performance validations matter before swapping a production model?", category: "Performance", answer: "Beyond aggregate accuracy, I would examine slice metrics for high-risk cohorts, tail latency under concurrent load, token throughput and cost per request, and regression tests on a frozen prompt suite including tool-use scenarios. For generative models, I would run automated quality rubrics and human spot checks on worst-case prompts. Promotion should require no sustained degradation on canaries and explicit comparison of resource utilization so autoscaling policies remain valid." }
    ]),
  },
  {
    id: "aiml-mlsd-continuous-data-knowledge-integration",
    title: "Continuous Data & Knowledge Integration",
    description: "Continuously ingest and integrate new data and knowledge sources.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Repeat",
    color: "bg-rose-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 39,
    content: emptyContent({ overview: "Continuously ingest and integrate new data and knowledge sources." }),
    article: toArticle([
      { type: 'paragraph', text: "RAG and agentic systems are only as current as the corpora they retrieve from. Continuous integration for knowledge means pipelines that ingest new documents, update structured records, and refresh vector indexes without taking the whole system offline. Architecture usually splits bulk backfills from incremental updates: message queues or change-data-capture streams carry create, update, and delete events from source systems into workers that normalize text, deduplicate by content hash, and enqueue embedding jobs." },
      { type: 'paragraph', text: "Document change detection compares source revision ids, content hashes, or extracted entity signatures so unchanged paragraphs do not force redundant embedding calls. Incremental indexing strategies include per-collection segment files with periodic compaction, dual-write to shadow indexes followed by atomic swap, or versioned index aliases in Elasticsearch-style clusters. Embedding update strategies range from full re-embed on any text change to selective re-embed when semantic diff exceeds a threshold, trading fidelity against API cost and latency during peak update windows." },
      { type: 'paragraph', text: "Stale data handling belongs in product policy as much as in engineering. Some answers should carry freshness timestamps or disclaimers when source-of-truth systems lag. Freshness SLAs define maximum acceptable lag per data class: regulatory filings might need sub-hour propagation while marketing blogs tolerate a day. Operational monitors track ingestion backlog depth, embedding queue age, and retrieval hit rates for newly added content to catch silent failures in connectors." },
      { type: 'paragraph', text: "Trade-offs include eventual consistency versus read-after-write expectations for internal tools. Stronger guarantees often require synchronous write paths that increase user-perceived latency on authoring flows. Another tension is global versus regional indexes: unified indexes simplify routing but may violate data residency; sharded indexes add routing complexity but align with compliance boundaries." },
      { type: 'qa', question: "How would you design an incremental embedding pipeline at scale?", category: "Architecture", answer: "I would use a durable event log for document changes, idempotent workers that chunk text deterministically, and a batching layer that groups embedding requests to respect rate limits and GPU batch sizes. Embeddings would write to a vector store with metadata for source version and content hash so queries can filter by recency or tenant. I would support full reindex jobs behind feature flags using the same code path to avoid divergence between incremental and bulk modes." },
      { type: 'qa', question: "What reliability patterns reduce user-visible stale answers?", category: "Reliability", answer: "I would expose document timestamps in retrieval results so the application layer can refuse to answer time-sensitive questions from old chunks, implement circuit breakers when connectors fail, and alert when median index lag exceeds SLA. Fallback paths might include live API lookup for authoritative records when the index is behind. Chaos testing on queue consumers catches poison messages that stall partitions." },
      { type: 'qa', question: "How do you operationalize freshness SLAs with non-engineering stakeholders?", category: "Operations", answer: "I would translate SLAs into measurable dashboards: time from publish in the CMS to retrievable chunk, and percentage of queries satisfied by documents newer than a threshold. Incident response playbooks would define when to pause retrieval features or show warnings. Capacity planning ties SLA to cost by modeling embedding spend under peak publishing events such as product launches or earnings releases." }
    ]),
  },
  {
    id: "aiml-mlsd-content-moderation-red-teaming",
    title: "Content Moderation & Red-Teaming",
    description: "Proactively test and moderate AI outputs for harmful content.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "ShieldAlert",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 40,
    content: emptyContent({ overview: "Proactively test and moderate AI outputs for harmful content." }),
    article: toArticle([
      { type: 'paragraph', text: "Red-teaming for language models is structured adversarial testing: skilled testers probe models and surrounding systems (prompt layers, tools, retrieval corpora) to elicit harmful, biased, or policy-violating outputs under realistic threat models. Methodologies range from time-boxed internal campaigns with defined success criteria to continuous purple-team exercises where defenders harden prompts and filters based on findings. Effective programs document attack taxonomies such as instruction hijacking, role-play evasion, encoding tricks, multi-turn escalation, and indirect injection via retrieved documents." },
      { type: 'paragraph', text: "Content classifiers and moderation APIs provide scalable first-line defense by scoring user inputs and model outputs for violence, sexual content, hate, self-harm, and custom enterprise policies. Architecturally, classifiers can run synchronously on the critical path with low-latency distilled models or asynchronously for audit and training data mining. Combining model-based moderation with rules for known patterns catches cases where neural scores are miscalibrated. Human-in-the-loop review handles edge cases, appeals, and high-stakes domains where false positives carry legal or reputational cost." },
      { type: 'paragraph', text: "Jailbreak prevention spans system prompts, output filters, tool permission boundaries, and retrieval hardening so untrusted text cannot override instructions. Attack taxonomies help prioritize mitigations: if indirect prompt injection is in scope, you invest in document sanitization, privilege separation for tools, and logging of tool arguments. Red-team findings should flow into regression suites of forbidden prompts and into metrics tracked over releases." },
      { type: 'paragraph', text: "Trade-offs include latency and cost from extra model calls versus residual risk from a single-layer defense in depth. Aggressive blocking improves safety metrics but can frustrate legitimate users in sensitive domains like medicine or journalism. Calibrating thresholds per surface area (public chat versus internal analyst assistant) aligns policy with context." },
      { type: 'qa', question: "How would you scope a red-team engagement for a customer-facing assistant with tool use?", category: "Safety", answer: "I would define prohibited behaviors aligned with product policy and regulation, enumerate assets including prompts, tools, and third-party APIs, and agree on reporting severity rubrics. Testers would attempt direct jailbreaks, multi-turn social engineering, and injection via user-supplied files and retrieved web snippets. I would require safe rollback channels, synthetic accounts, and logging that captures attack traces without storing unnecessary user PII. Outcomes feed prioritized fixes and automated regression tests before any public disclosure." },
      { type: 'qa', question: "What methodology separates one-off red teams from continuous assurance?", category: "Methodology", answer: "One-off engagements produce a snapshot assessment before launch, while continuous programs schedule rotating testers, integrate automated adversarial generators, and track time-to-remediate by severity. Continuous methodology ties findings to version control so each model or prompt change retriggers a minimum battery of probes. Metrics include open critical issues, repeat-offense rate on known attacks, and coverage of new policy areas as regulations evolve." },
      { type: 'qa', question: "Where do moderation classifiers sit in the inference architecture?", category: "Architecture", answer: "A common pattern is pre-classification on user input to block or rewrite before the main model runs, plus post-classification on the draft output before delivery, with optional escalation to a stricter model or human queue. Classifiers should receive the same context the policy cares about, including conversation history summaries where relevant. The architecture must handle timeouts gracefully, degrade to safe defaults when the moderation service is unavailable, and avoid leaking blocked content into logs accessible to low-privilege operators." }
    ]),
  },
  {
    id: "aiml-mlsd-bias-toxicity-detection",
    title: "Bias & Toxicity Detection",
    description: "Detect and mitigate bias, toxicity, and fairness issues in model outputs.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Siren",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 41,
    content: emptyContent({ overview: "Detect and mitigate bias, toxicity, and fairness issues in model outputs." }),
    article: toArticle([
      { type: 'paragraph', text: "Bias measurement begins with clarifying the decision or utterance being evaluated and the populations that matter for equity. Frameworks such as fairness indicators encourage reporting metrics across slices defined by legally protected or product-relevant attributes, while acknowledging that proxy variables are imperfect. Toxicity detection often combines general services like Perspective API with domain-specific fine-tuned classifiers because generic models miss coded language, reclaimed slurs in acceptable contexts, and multilingual harassment patterns." },
      { type: 'paragraph', text: "Fairness metrics attach to the task: demographic parity compares positive outcome rates across groups; equalized odds requires similar true positive and false positive rates; calibration asks whether predicted probabilities mean the same thing across groups. Generative systems add toxicity rate, stereotype reinforcement, and refusal inconsistency as operational metrics. Intersectional analysis looks beyond single-axis slices at combinations such as gender and geography, which can hide harms visible only when groups are small and variance is high." },
      { type: 'paragraph', text: "Debiasing techniques span data rebalancing, reweighting, adversarial debiasing during training, post-processing thresholds per group, and constrained optimization. For LLMs, mitigation also includes instruction tuning on refusal behaviors, curated counter-stereotypical demonstrations, and retrieval filters that block biased source documents. None of these remove societal bias entirely; they shift trade-offs between group error rates, overall accuracy, and transparency." },
      { type: 'paragraph', text: "Trade-offs include measurement burden versus statistical power: rare slices produce unstable estimates without large evaluation sets. Toxicity classifiers carry false positive costs that disproportionately affect dialects and marginalized communities if training data reflects majority norms. Teams should publish limitations, run human audits on classifier errors, and avoid using fairness metrics as a single pass or fail without context." },
      { type: 'qa', question: "How do demographic parity and equalized odds differ in practice?", category: "Fairness", answer: "Demographic parity asks that the proportion of positive predictions match across groups regardless of underlying base rates, which can force unequal true positive rates when prevalence differs. Equalized odds requires that true positives and false positives are comparable across groups, which is often closer to equalizing mistakes when the label is meaningful. Choosing between them depends on whether equal selection rates or equal error treatment aligns better with policy and whether base rate differences reflect legitimate signal or historical inequity in labels." },
      { type: 'qa', question: "What are pitfalls when deploying a toxicity classifier in production?", category: "Detection", answer: "Classifiers can inherit annotator bias, perform poorly on multilingual or informal text, and be gamed with obfuscation. Latency and availability affect user experience if moderation is synchronous. Threshold tuning must consider false positives that silence valid speech and false negatives that allow harm. I would monitor slice metrics, sample human review of borderline scores, and version the classifier with rollback paths when drift appears after internet language evolves." },
      { type: 'qa', question: "How would you approach intersectional fairness evaluation?", category: "Methodology", answer: "I would predefine intersectional cells with minimum sample thresholds and use Bayesian or bootstrap confidence intervals to communicate uncertainty. Where counts are too small, I would combine careful qualitative review with synthetic stress tests rather than over-trusting point estimates. I would document which intersections are out of scope for quantitative claims due to data sparsity and work with domain experts to prioritize high-risk combinations for deeper study and mitigation design." }
    ]),
  },
  {
    id: "aiml-mlsd-access-control-rbac",
    title: "Access Control & RBAC",
    description: "Implement role-based access control for models, data, and endpoints.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Lock",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 42,
    content: emptyContent({ overview: "Implement role-based access control for models, data, and endpoints." }),
    article: toArticle([
      { type: 'paragraph', text: "Role-based access control maps identities to roles such as data scientist, MLOps engineer, or service account, and roles to permissions on resources like datasets, training jobs, model registry entries, and deployment stages. ML systems complicate classic RBAC because the same vector store might serve both internal research and customer-isolated indexes, and because LLM endpoints effectively execute arbitrary natural language that can request sensitive tools. Policies must cover who can trigger training on production-tenant data, who can promote models, and who can read evaluation artifacts that may contain PII samples." },
      { type: 'paragraph', text: "Model endpoint authentication typically combines mutual TLS or OAuth between services with scoped API keys for external developers. Short-lived tokens reduce blast radius when a key leaks, and audience restrictions prevent a token minted for one microservice from being replayed against the inference gateway. Data access policies align with row-level security in warehouses and with bucket policies in object stores so training jobs receive narrow temporary credentials via workload identity rather than long-lived secrets on VMs." },
      { type: 'paragraph', text: "Service mesh authorization (for example Istio or Envoy RBAC filters) enforces which services may call an inference pod, which HTTP routes are allowed, and which headers must be present for tenant routing. For LLM tool use, least privilege means each tool adapter declares allowed operations and parameter schemas; the runtime rejects tool calls that exceed the session role, and secrets for downstream systems never enter the model context. API key management belongs in a vault or managed secrets store with rotation, break-glass procedures, and audit trails for issuance." },
      { type: 'paragraph', text: "Trade-offs include coarse roles that are easy to administer but over-permission many users versus fine-grained attribute-based policies that are accurate but complex to reason about. Centralized identity providers help, but latency-sensitive inference paths must cache authorization decisions carefully without stale permissions after revocation." },
      { type: 'qa', question: "How would you secure a multi-tenant model serving API?", category: "Security", answer: "I would require tenant-scoped tokens with explicit audience, enforce quotas and rate limits per tenant, isolate tenant-specific adapters or LoRA weights in separate storage namespaces, and validate that retrieval queries cannot cross tenant boundaries. Logging would include tenant id and request id without storing sensitive payloads unless policy allows. I would run periodic penetration tests on authentication bypass paths and monitor for anomalous token usage patterns indicative of key theft." },
      { type: 'qa', question: "Where does RBAC plug into the MLOps control plane?", category: "Architecture", answer: "RBAC applies to experiment tracking, notebook platforms, feature stores, registries, and orchestrators like Kubernetes or workflow engines. The architecture should propagate the caller identity through the pipeline so data access decisions use the human or service principal that initiated the job, not a shared superuser. Service accounts for training should be distinct from serving accounts, with promotion workflows granting time-bounded elevation only when needed." },
      { type: 'qa', question: "What implementation details reduce LLM tool abuse risk?", category: "Implementation", answer: "Implement tools as server-side functions with fixed allowlists rather than arbitrary shell access, validate arguments with strict JSON schema, and bind each session to a credential vault handle that resolves secrets out-of-band. Log tool invocations with structured fields for auditing. Cap recursion depth and parallel tool calls, and require human confirmation for irreversible actions such as financial transfers or mass emails when policies demand it." }
    ]),
  },
  {
    id: "aiml-mlsd-compliance-audit-logging",
    title: "Compliance & Audit Logging",
    description: "Meet GDPR, AI Act, and SOC2 requirements with comprehensive audit trails.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "ScrollText",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 43,
    content: emptyContent({ overview: "Meet GDPR, AI Act, and SOC2 requirements with comprehensive audit trails." }),
    article: toArticle([
      { type: 'paragraph', text: "Compliance for AI systems intersects privacy law, sector regulation, and voluntary assurance frameworks. GDPR influences how personal data in training sets, logs, and prompts is processed, including legal bases, data minimization, cross-border transfers, and data subject rights such as access and erasure that conflict with immutable training snapshots unless engineered with pseudonymization and reversible mappings. The EU AI Act introduces risk-tiered obligations for high-risk systems, including documentation, human oversight, and post-market monitoring expectations that shape logging and model governance." },
      { type: 'paragraph', text: "SOC 2 oriented controls emphasize access logging, change management, vulnerability management, and availability monitoring rather than prescribing exact AI metrics. Immutable audit logs append-only to tamper-evident storage or WORM buckets support investigations into who promoted a model, who exported a dataset, or which administrator changed a safety threshold. Retention policies balance forensic value against storage cost and privacy: shorter retention for raw prompts containing PII, longer retention for aggregated metrics and cryptographic hashes of configurations." },
      { type: 'paragraph', text: "Right-to-explanation requirements vary by jurisdiction and often do not mandate a single interpretability technique, but they push teams toward logging inputs, outputs, model version, and material factors considered in automated decisions, plus human-readable summaries where feasible. Model decision logging for scoring systems might store top features from an explainability method with known limitations; for generative systems, logging focuses on policy versions, retrieval sources, and tool traces rather than claiming step-by-step causal reasoning." },
      { type: 'paragraph', text: "Trade-offs include comprehensive logging that aids audits but increases privacy risk if logs become a new sensitive datastore. Pseudonymization, field-level encryption, and purpose-limited access roles mitigate that tension. Another challenge is reconciling erasure requests with model unlearning realities; operational responses may rely on suppression lists at inference time and retraining schedules rather than perfect parameter-level deletion." },
      { type: 'qa', question: "What GDPR considerations apply to LLM chat logs used for improvement?", category: "Compliance", answer: "You need a lawful basis such as consent or legitimate interests, clear notice to users, and often a way to opt out of training use. Logs may contain special category data inferred from conversation, which triggers additional safeguards. Cross-border transfers require appropriate mechanisms such as standard contractual clauses. Retention should be minimized and aligned with documented purposes; fulfilling erasure may require deleting or anonymizing stored transcripts and excluding them from future training sets even if historical models cannot be fully unlearned." },
      { type: 'qa', question: "How would you design governance for EU AI Act high-risk classification?", category: "Governance", answer: "I would maintain a risk register linking product use cases to Annex categories, assign accountable owners for conformity documentation, and implement required technical measures such as logging, human oversight interfaces, and incident reporting workflows. Change control would capture model and data updates that materially affect risk. I would coordinate legal review for labeling accuracy claims and ensure post-market monitoring feeds back into the risk assessment on a defined cadence." },
      { type: 'qa', question: "What belongs in an immutable audit log for model promotion?", category: "Architecture", answer: "Include actor identity, role, timestamp, source and destination stages, model version ids, checksums, linked evaluation report ids, approval ticket references, and hashes of safety configuration. The log stream should be append-only with cryptographic chaining or external notarization where threat models warrant it. Read APIs should enforce least privilege and support efficient queries by model id and time range for auditors without exposing customer content inline unless strictly necessary and separately authorized." }
    ]),
  },
  {
    id: "aiml-mlsd-data-privacy-pii-handling",
    title: "Data Privacy & PII Handling",
    description: "Detect, mask, and manage personally identifiable information in ML pipelines.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Fingerprint",
    color: "bg-teal-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 44,
    content: emptyContent({ overview: "Detect, mask, and manage personally identifiable information in ML pipelines." }),
    article: toArticle([
      { type: 'paragraph', text: "PII detection stacks combine fast heuristics such as regex and dictionary checks for emails and phone numbers with named-entity recognition models tuned for person, location, and organization spans in multiple languages. LLM-assisted detection can catch contextual leaks that regex misses but should not be the only gate in high-risk settings because models hallucinate labels and may themselves see sensitive text. A practical architecture runs deterministic detectors first, escalates uncertain spans to stronger models or human review queues, and stores confidence scores for tuning thresholds." },
      { type: 'paragraph', text: "Data masking and anonymization reduce exposure in logs, training exports, and analytics. Techniques include token replacement with stable pseudonyms for longitudinal joins, generalization of quasi-identifiers like birth year buckets, and redaction of spans in free text used for display. Differential privacy adds calibrated noise to aggregates or training objectives so individual contributions are plausibly deniable at a chosen epsilon budget; it is powerful for reporting and some federated settings but complicates utility and requires careful composition across repeated queries." },
      { type: 'paragraph', text: "Training data privacy extends to how third-party scrapes, user uploads, and enterprise documents are labeled for consent and segregation from internet-scale pretraining mixes. Prompt injection aimed at PII extraction tricks assistants into repeating system prompts, tool outputs, or memories that contain secrets. Defenses include minimizing sensitive content in prompts, separating secrets from model-visible context, output filters that block structured secret patterns, and training staff not to paste credentials into chat interfaces." },
      { type: 'paragraph', text: "Trade-offs include aggressive redaction that breaks downstream model quality versus permissive pipelines that leak in embeddings or memorization. Data minimization means collecting only fields needed for the task and setting TTLs on caches holding user context. Differential privacy trades accuracy for guarantees, which may be unacceptable for small cohort analytics unless budgets are tuned with expert review." },
      { type: 'qa', question: "How would you build a layered PII detection pipeline for user-generated documents?", category: "Privacy", answer: "I would scan on upload with regex and checksum blocklists for known secret formats, run a NER model with language detection per segment, and route high-risk documents to stricter models or manual review based on policy. Detected spans would be written to storage with encryption and access controls, with hashed fingerprints for deduplication. I would continuously evaluate precision and recall on labeled corpora and monitor for new leak patterns after incidents." },
      { type: 'qa', question: "What implementation choices support safe logging around LLMs?", category: "Implementation", answer: "Implement structured scrubbers on the logging path that remove or tokenize PII before logs hit centralized systems, default to payload omission for LLM traces, and use allowlisted fields for debugging. Separate environments so production keys never appear in staging prompts. Use automated tests that fail builds if sample logs contain patterns resembling secrets, and rotate keys promptly if accidental logging occurs." },
      { type: 'qa', question: "How does differential privacy interact with ML training at a high level?", category: "Architecture", answer: "In deep learning, common approaches add noise to gradients or use private aggregation of teacher ensembles to limit leakage from any single training example. The architecture must track cumulative privacy spend across training updates and releases, because repeated publication consumes the budget. For analytics APIs, differential privacy engines sit between analysts and databases, enforcing per-query noise and enforcing budgets per user or organization to prevent reconstruction attacks." }
    ]),
  },
  {
    id: "aiml-mlsd-containerization-orchestration",
    title: "Containerization & Orchestration",
    description: "Package and orchestrate ML workloads with Kubernetes and serverless.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Container",
    color: "bg-emerald-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 45,
    content: emptyContent({ overview: "Package and orchestrate ML workloads with Kubernetes and serverless." }),
    article: toArticle([
      { type: 'paragraph', text: "Containers give ML teams a portable unit of deployment: the same image that passed integration tests in CI can run on a developer laptop, a GPU node in a private cluster, or a managed service in the cloud. For inference, that repeatability shortens incident response because you can bisect regressions by image digest rather than chasing host-level drift in CUDA drivers, Python packages, and system libraries. For training, images still help, but the dominant concerns shift to data locality, checkpoint durability, and job restart semantics when workers are preempted." },
      { type: 'paragraph', text: "Kubernetes is the de facto orchestration layer for teams that need multi-tenant clusters, fine-grained placement, and declarative rollouts. GPU workloads add scheduling dimensions beyond CPU and memory: you must align device plugin reported capacity with node taints, tolerations, and often specialized operators that manage node pools, driver versions, and topology-aware placement for multi-GPU nodes. Community and vendor operators (for example NVIDIA GPU Operator patterns) automate driver and runtime lifecycle so cluster upgrades do not silently strand inference pods waiting for compatible runtimes. Knative, Cloud Run, and similar serverless abstractions trade some control for rapid scale-to-zero and request-based autoscaling, which suits bursty inference if cold start latency and maximum concurrency limits fit your SLO profile." },
      { type: 'paragraph', text: "Resource requests and limits for GPUs are not interchangeable with CPU semantics. Under-scheduling leads to noisy neighbor contention on VRAM and PCIe bandwidth; over-reserving strands expensive silicon. Multi-container pods are useful when a primary model server benefits from a tightly coupled sidecar for observability export, authorization token minting, or gRPC/HTTP transcoding without extra network hops. Init containers are a natural place to pull large model weights from object storage, verify checksums, and warm filesystem caches before the main process binds its port, so the service does not advertise readiness until weights are local and integrity-checked. The recurring trade-off is coupling versus independence: sidecars simplify operations until their failure modes become entangled with the model container’s lifecycle, at which point separate Deployments with explicit service mesh policy may be clearer." },
      { type: 'qa', question: "When would you put model download logic in an init container instead of the main application container?", category: "Infrastructure", answer: "Use an init container when you want strict ordering so the model process never starts listening until artifacts are present and verified, when you want different security contexts or credentials for pull-only storage access, or when you want to reuse a minimal, stable base image for the runtime while isolating heavy download tooling. The main container then focuses on serving logic and can restart without re-downloading if you mount a shared volume populated by the init step." },
      { type: 'qa', question: "How do Kubernetes resource requests interact with GPU scheduling in practice?", category: "Architecture", answer: "CPU and memory requests influence the scheduler’s fit, but GPUs are typically exposed as extended resources such as nvidia.com/gpu, and you must request integer or fractional units according to your device plugin and partitioning scheme. The scheduler also needs awareness of topology, shared multi-instance GPU slices if enabled, and taints that reserve nodes for training versus inference. Misconfigured requests can schedule a pod onto a node that reports GPUs while still failing at runtime if driver versions, CUDA capabilities, or MIG profiles do not match what the image expects." },
      { type: 'qa', question: "What operational risks appear when mixing Knative-style scale-to-zero with low-latency inference SLOs?", category: "Operations", answer: "Cold starts include image pull, runtime initialization, model load from disk or remote storage, and JIT compilation for some frameworks, any of which can breach tail latency budgets. Concurrency limits and surge handling during flash traffic can queue requests unless you keep minimum instances or predictive warm pools. You also need clear metrics on startup phases, budgets for always-warm canary pools, and fallback routing to a steady-state deployment when serverless capacity is throttled." }
    ]),
  },
  {
    id: "aiml-mlsd-gpu-tpu-resource-management",
    title: "GPU/TPU Resource Management",
    description: "Optimize GPU allocation, sharing, and scheduling for cost efficiency.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Cpu",
    color: "bg-lime-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 46,
    content: emptyContent({ overview: "Optimize GPU allocation, sharing, and scheduling for cost efficiency." }),
    article: toArticle([
      { type: 'paragraph', text: "Accelerator capacity is almost always the binding constraint in modern ML infrastructure. GPUs expose several sharing models: time-slicing interleaves processes on the same device with context switches, which raises throughput for many small jobs but harms predictable latency. NVIDIA Multi-Process Service reduces launch overhead for concurrent CUDA contexts on a single GPU, which helps colocated inference microservices if memory headroom exists. Multi-Instance GPU hardware-splits a physical card into isolated instances with dedicated memory and compute slices, improving isolation compared with pure software oversubscription at the cost of rigid partition sizes." },
      { type: 'paragraph', text: "Fractional GPU allocation in Kubernetes is usually implemented via MIG profiles, time-slicing configuration on the device plugin, or vendor-specific virtual GPU mechanisms. The architecture decision is whether you optimize for average utilization or worst-case latency. Training jobs often want whole devices or tightly coupled multi-GPU pods with NVLink-friendly topology; inference tiers may accept smaller slices if batching and KV-cache residency still fit in the allocated VRAM. TPUs differ in pod-shaped reservations and compile-time graph placement, but the same product tension appears: bin-pack for efficiency versus reserve headroom for tail latency and compilation and data loading spikes." },
      { type: 'paragraph', text: "VRAM management spans model weights, optimizer states in training, activation checkpoints, and runtime caches such as attention key-value stores in large language model serving. Spot and preemptible instances discount cost dramatically but require checkpoint frequency tuned to expected lifetime distributions and idempotent data staging so a restart does not corrupt shared filesystems. Monitoring should combine hardware counters with application signals: GPU utilization percentage alone is misleading when a kernel is memory-bound, so track SM occupancy, PCIe throughput, power cap throttling, and out-of-memory events correlated with queue depth." },
      { type: 'qa', question: "Why is high GPU utilization not always a healthy sign for an inference service?", category: "Resources", answer: "A saturated GPU can mean you are batching aggressively and meeting throughput goals, but it can also hide memory-bound kernels where compute units stall waiting for DRAM or HBM, or indicate queueing that inflates end-to-end latency. For latency-sensitive APIs you often prefer moderate utilization with controlled batch sizes, explicit concurrency caps, and metrics that tie device activity to per-request latency percentiles rather than averages." },
      { type: 'qa', question: "How do spot or preemptible accelerators change how you design distributed training?", category: "Cost", answer: "You design for elasticity: frequent checkpoints to durable object storage, elastic worker groups that tolerate shrinkage, and rendezvous protocols that do not assume a fixed world size for the entire epoch. You may mix a small on-demand coordinator or parameter server tier with spot workers, and you price in recomputation of the last checkpoint window when nodes disappear. Cost savings only hold if wasted partial epochs and engineering toil do not erase the discount." },
      { type: 'qa', question: "When would you choose MIG over time-slicing for colocated inference workloads?", category: "Architecture", answer: "MIG provides stronger isolation of memory and compute so one tenant’s peak allocation cannot exhaust another’s VRAM as easily, which matters for multi-tenant SaaS with contractual limits. Time-slicing is more flexible for highly variable micro-services that rarely need a full card but can tolerate less deterministic latency. MIG’s downside is coarse slice sizes and operational complexity reprovisioning profiles when model families change." }
    ]),
  },
  {
    id: "aiml-mlsd-multi-region-edge-deployment",
    title: "Multi-Region / Edge Deployment",
    description: "Deploy models closer to users with multi-region and edge strategies.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Globe",
    color: "bg-amber-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 47,
    content: emptyContent({ overview: "Deploy models closer to users with multi-region and edge strategies." }),
    article: toArticle([
      { type: 'paragraph', text: "Global ML products face a geographic tension: users expect low latency, regulators expect data to stay in defined jurisdictions, and engineering teams want a single logical model version without running twelve divergent forks. A common pattern is to replicate inference endpoints per region behind geo-DNS or anycast, backed by regional object stores or container registries that host the same versioned artifacts. Training and batch analytics may remain centralized where raw data is allowed to accumulate, while online inference consumes only derived features or redacted payloads permitted to cross borders." },
      { type: 'paragraph', text: "Edge inference pushes computation toward Points of Presence, cellular gateways, or on-prem appliances. Runtimes such as ONNX Runtime and TensorFlow Lite target constrained devices with quantized weights, operator fusion, and reduced precision execution. The architecture work is not only shrinking the model but also defining update channels: differential bundles, staged rollouts, and integrity verification when devices are intermittently connected. Latency-aware routing at the edge must integrate with health checks that distinguish regional outage from model-specific failure, otherwise traffic shifts can amplify bad deployments." },
      { type: 'paragraph', text: "Data residency rules may forbid storing prompts, embeddings, or logs in foreign regions even when the model weights are identical worldwide. That implies regional logging sinks, separate vector indexes built only on locally ingested documents, and clear contracts for what metadata replication is allowed. Model synchronization across regions can use immutable version tags in artifact storage plus control-plane APIs that advertise which digest is active per region, optionally with deliberate staggered promotion to limit blast radius. Content delivery networks help distribute static bundles and WASM or JS inference assets to browsers, but large binary checkpoints usually stay off generic CDNs unless you have signed URLs, byte-range integrity, and abuse protections tuned for multi-gigabyte downloads." },
      { type: 'qa', question: "How would you keep model versions consistent across regions without forcing a single global failure domain?", category: "Architecture", answer: "Treat each region as an independent deployment target that pins to immutable artifact digests promoted through a control plane. Use a manifest that lists approved versions per region and environment, automate promotion with gates, and allow rollback by repointing the manifest. Avoid shared mutable storage for weights; replicate objects asynchronously but only mark traffic after checksum verification and smoke tests complete in that geography." },
      { type: 'qa', question: "What are the main latency pitfalls when serving from the edge versus a regional data center?", category: "Latency", answer: "Cold caches on sparse POPs, TLS handshakes without session resumption, oversized payloads crossing last-mile networks, and device thermal throttling all dominate over raw operator latency. You must measure end-to-end time including client-side preprocessing, choose quantization and sequence limits appropriate for the link, and place authentication and tokenization close to where data is allowed to exist to avoid extra round trips across slow paths." },
      { type: 'qa', question: "How does data residency affect a retrieval-augmented generation system that indexes enterprise documents?", category: "Compliance", answer: "Embeddings and chunk text for each tenant must reside in approved regions, which often means dedicated clusters per jurisdiction, no cross-border vector search unless contracts allow it, and audit logs that prove query and index data never left the zone. Replicated global indexes are only viable when the underlying content itself is globally portable; otherwise you duplicate ingestion pipelines and maintain separate freshness SLAs per region." }
    ]),
  },
  {
    id: "aiml-mlsd-rollback-blue-green-deployment",
    title: "Rollback & Blue-Green Deployment",
    description: "Safely roll out model updates with blue-green and canary strategies.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Undo2",
    color: "bg-orange-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 48,
    content: emptyContent({ overview: "Safely roll out model updates with blue-green and canary strategies." }),
    article: toArticle([
      { type: 'paragraph', text: "Blue-green deployment for ML keeps two parallel stacks: blue serves production traffic while green hosts the candidate model, tokenizer changes, prompt template revisions, or feature extraction updates. A router or load balancer flips traffic atomically once health checks and synthetic canaries pass. Unlike stateless microservices, ML rollouts must validate not only error rates but also distribution shift in outputs, latency regressions from larger graphs, and memory footprint changes that only appear under realistic batch sizes." },
      { type: 'paragraph', text: "Canary releases send a small percentage of live traffic to the new version while comparing golden metrics: HTTP 5xx rates, timeout ratios, business KPIs such as conversion, and model-specific scores like toxicity classifier pass rates or embedding drift against a reference corpus. Shadow mode duplicates requests to the new stack without returning its responses to users, which avoids user-visible risk but still exercises real inputs if privacy policy allows storing mirrored payloads securely. Shadow traffic must not double-charge paid upstream APIs or write side effects unless you explicitly neutralize those code paths." },
      { type: 'paragraph', text: "Traffic shifting strategies include stepwise percentage ramps, geography-based canaries, and cohort routing for internal users first. Automated rollback triggers should combine fast signals such as error spikes with slower signals such as statistically significant quality drops detected via logged judgments or delayed feedback. Feature flags decouple binary deployment from behavioral exposure so you can deploy artifacts widely but gate model IDs, temperature defaults, or retrieval corpora per tenant. The trade-off is configuration complexity: flags that interact badly can produce states nobody tested, so treat flag matrices as part of release engineering with contract tests." },
      { type: 'qa', question: "Why is a low HTTP error rate insufficient to declare an ML canary successful?", category: "Deployment", answer: "Models can return syntactically valid responses that are systematically wrong, biased, or empty in ways that damage user trust without raising transport errors. You need outcome metrics, human evaluation samples, automated quality checks on outputs, and comparisons to baseline distributions for length, refusal rate, and tool-call success. Roll forward only when those secondary signals remain within agreed bounds." },
      { type: 'qa', question: "How does shadow traffic help, and what are its limits?", category: "Reliability", answer: "Shadow requests validate runtime stability, autoscaling behavior, and integration with dependencies under production load shapes without user impact. Limits include inability to measure user satisfaction on the shadow path, potential privacy issues if payloads are sensitive, doubled compute cost, and blind spots if shadow consumers lack idempotency or mutate state you forgot to disable. Always tag shadow spans in tracing to avoid polluting production analytics." },
      { type: 'qa', question: "What operational playbooks should exist around automated rollback for models?", category: "Operations", answer: "Define explicit owners and thresholds per metric, ensure rollbacks revert not only routing weights but also caches and prompt bundles that were co-versioned, and verify observability distinguishes rollback events from unrelated incidents. Post-incident reviews should capture whether triggers were too sensitive causing churn or too slow allowing damage, and update SLO-based gates accordingly. Document manual override paths when automation conflicts with a planned experiment." }
    ]),
  },
  {
    id: "aiml-mlsd-rate-limiting-throttling",
    title: "Rate Limiting & Throttling",
    description: "Protect services with rate limiting, throttling, and backpressure.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Gauge",
    color: "bg-rose-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 49,
    content: emptyContent({ overview: "Protect services with rate limiting, throttling, and backpressure." }),
    article: toArticle([
      { type: 'paragraph', text: "Inference endpoints sit on the boundary between bursty human traffic and expensive accelerator time. Rate limiting answers who is allowed to consume scarce capacity and at what steady-state rate. Token bucket algorithms allow controlled bursts while preserving a long-term average, which maps well to conversational workloads where users send bursts of short messages. Sliding window counters approximate smoother enforcement over fixed wall-clock windows and reduce the cliff-edge behavior of naive fixed windows at the cost of more memory per key for fine-grained buckets." },
      { type: 'paragraph', text: "Per-user and per-tenant limits express fairness and commercial entitlements: free tiers receive lower sustained rates than enterprise contracts. Priority queues let you segregate interactive traffic from batch scoring, or elevate paid customers during congestion, but you must cap queue depth or worst-case latency explodes while memory grows unbounded. Admission control at the gateway is only the first layer; internal stages such as embedding services and vector databases need aligned budgets or a single abusive tenant can bypass external limits through fan-out." },
      { type: 'paragraph', text: "Backpressure propagates overload signals upstream: when the model server cannot accept more work, it should shed load predictably rather than accept everything and collapse. Patterns include fast rejection with Retry-After headers, load shedding based on queue wait time thresholds, and circuit breakers when downstream dependencies fail. Graceful degradation might reduce max output tokens, disable optional retrieval, or fall back to a smaller model tier. Queue-based load leveling with asynchronous workers smooths spikes but shifts the product semantics toward eventual responses, which only works if clients support polling or webhooks." },
      { type: 'qa', question: "Compare token bucket and sliding window rate limiting for a public inference API.", category: "Reliability", answer: "Token buckets naturally tolerate short bursts up to bucket capacity while enforcing an average refill rate, which feels responsive for humans. Sliding windows track usage across recent sub-windows to avoid the reset spike exploit of coarse fixed windows, giving fairer enforcement over time at higher storage cost. Many gateways combine both ideas or use approximate algorithms at very large cardinality." },
      { type: 'qa', question: "Where should rate limits be enforced in a system with an API gateway, a BFF, and a GPU model server?", category: "Architecture", answer: "Enforce coarse commercial limits at the edge gateway for early rejection and abuse protection, enforce semantic and auth-aware limits at the BFF where tenant identity is trusted, and enforce final capacity limits at the model server or sidecar proxy where you observe actual GPU queue depth. Each layer needs consistent error contracts and telemetry so operators see which stage throttled a request." },
      { type: 'qa', question: "How do you prevent priority queues from starving low-priority work entirely?", category: "Performance", answer: "Use weighted fair queuing or guaranteed minimum service slices so background jobs still make progress, cap high-priority queue depth to avoid memory blowups, and apply aging policies that promote long-waiting low-priority tasks occasionally. Monitor p99 wait time per class and alert when low-priority starvation violates internal SLOs even if premium traffic looks healthy." }
    ]),
  },
  {
    id: "aiml-mlsd-user-feedback-collection",
    title: "User Feedback Collection",
    description: "Design feedback mechanisms — thumbs up/down, corrections, and ratings.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "Heart",
    color: "bg-violet-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 50,
    content: emptyContent({ overview: "Design feedback mechanisms — thumbs up/down, corrections, and ratings." }),
    article: toArticle([
      { type: 'paragraph', text: "Explicit feedback such as thumbs up and down, star ratings, or structured corrections gives the clearest training signal when volume and bias are managed. Thumbs are cheap to collect but ambiguous: a downvote may reflect model error, policy disagreement, or user frustration unrelated to quality. Richer corrections, where users rewrite an answer or highlight a wrong span, are gold for supervised fine-tuning but require careful UX to avoid exhausting contributors. Ratings on dimensions such as helpfulness versus safety help disentangle failure modes that binary labels collapse." },
      { type: 'paragraph', text: "Implicit behavioral signals scale where explicit labels do not. Copy events, dwell time, follow-up reformulations, abandonment mid-session, and regenerate clicks all hint at dissatisfaction or exploration. These proxies correlate with quality but are noisy and context-dependent; a user who copies text may be delighted or may be archiving a bad answer for escalation. Instrumentation should log minimal identifiers, session correlation keys, and model version hashes so analysts can join feedback to prompts without retaining more raw content than policy allows." },
      { type: 'paragraph', text: "A durable feedback store feeds analytics dashboards, human review queues, and automated retraining or preference learning datasets. The architecture typically lands events in an append-only log or warehouse, deduplicates spam, and applies consent filters before export to labeling tools. Privacy considerations dominate: inform users what is logged, minimize retention of prompts containing personal data, support deletion requests that propagate to derived datasets, and segregate environments so staging feedback never poisons production training corpora." },
      { type: 'qa', question: "How would you design a UX that increases high-quality explicit feedback without annoying users?", category: "Design", answer: "Ask for feedback at natural completion points, default to low-friction options like thumbs with optional free text, and explain briefly how corrections help. Offer progressive disclosure: after a negative signal, invite a structured reason list before freeform input. Close the loop selectively, for example thanking internal testers, without creating pressure that biases ratings upward." },
      { type: 'qa', question: "What architectural components sit between the client and a training pipeline for user feedback?", category: "Architecture", answer: "An ingestion API or SDK that validates schema and authentication, a streaming bus or batch landing zone, PII scanning and redaction workers, a feature store or warehouse for aggregates, and workflow gates that promote curated slices into labeling or fine-tuning jobs. Each hop should attach model version, prompt template hash, and retrieval corpus identifiers for reproducibility." },
      { type: 'qa', question: "What privacy pitfalls appear when using implicit signals for model training?", category: "Privacy", answer: "Users may not expect silent logging of partial drafts or sensitive queries, and inferred labels can encode surveillance-like inferences. Retention without purpose limitation violates many regulations, and cross-user joins can re-identify individuals if session keys leak. Mitigate with transparent settings, aggressive minimization, differential access controls for derived datasets, and periodic audits that verify consent scopes match actual exports." }
    ]),
  },
  {
    id: "aiml-mlsd-reinforcement-preference-learning",
    title: "Reinforcement & Preference Learning",
    description: "Apply RLHF, DPO, and preference optimization from collected feedback.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "TrendingUp",
    color: "bg-indigo-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 51,
    content: emptyContent({ overview: "Apply RLHF, DPO, and preference optimization from collected feedback." }),
    article: toArticle([
      { type: 'paragraph', text: "Reinforcement learning from human feedback starts from a supervised policy and improves it using a reward model trained to predict human preferences between pairs of outputs for the same prompt. The policy is then updated with a policy-gradient style optimizer, often Proximal Policy Optimization, that increases probability on actions judged better while a KL penalty to a reference model stops the policy from collapsing into reward hacking or incoherent text. The pipeline spans costly human labeling, reward model generalization outside the comparison distribution, and careful evaluation because reward scores are not automatically aligned with user happiness." },
      { type: 'paragraph', text: "Direct Preference Optimization reframes the problem: instead of fitting an explicit scalar reward and running RL, DPO optimizes the policy directly from preference pairs using a closed-form objective derived under a Bradley-Terry preference model and a KL-regularized RL target. Practically, DPO can be more stable to tune than PPO for modest datasets because it removes the inner loop of sampling from the evolving policy for reward scoring, but it still demands diverse, high-quality preference data and can overfit when the reference model is weak or comparisons are noisy." },
      { type: 'paragraph', text: "Preference data collection must cover edge cases the base model mishandles: refusals, tool use, long-context faithfulness, and multilingual fairness. Online learning periodically samples from the live policy and asks labelers to compare fresh rollouts, which tracks real drift but risks exposing users to worse intermediates unless gated. Offline learning uses frozen logs, which is safer but stale. KL constraints exist because unconstrained maximization of a proxy reward exploits misspecified regions of the reward model. PPO offers flexible credit assignment across long completions but brings variance, hyperparameter sensitivity, and infrastructure complexity. DPO simplifies training loops at the cost of less explicit control over exploration and sometimes weaker performance when preference data is narrow." },
      { type: 'qa', question: "What failure modes appear if the reward model is trained on narrow comparison data?", category: "Training", answer: "The policy optimizes against a misspecified surrogate and may produce high reward text that humans dislike, including verbosity, hedging, or sycophancy if those correlated with positive labels in the dataset. Out-of-distribution prompts expose brittle generalization. Mitigations include broader sampling strategies, ensemble or uncertainty-aware rewards, periodic human audits, and combining reward training with hard constraints or rule checks." },
      { type: 'qa', question: "When might you still choose PPO-style RLHF over DPO for a product iteration?", category: "Methodology", answer: "PPO can help when you need intermediate reward shaping, multi-step tool environments, or non-differentiable components where scalar rewards come from simulators or hybrid pipelines. If your team already invested in robust RL infrastructure and you have very large interactive budgets, PPO may squeeze incremental gains. If your goal is stable alignment updates from offline preference logs with smaller engineering surface area, DPO is often faster to iterate." },
      { type: 'qa', question: "How does KL regularization to a reference model affect the system architecture?", category: "Architecture", answer: "You must version and serve the frozen reference alongside checkpoints for loss computation or implicit regularization terms, doubling storage and sometimes compute during training. Inference systems rarely need the reference unless you deploy ensembles or monitor divergence metrics in production. Training jobs should log effective KL and early-stop when divergence blows up, and artifact pipelines should tie policy weights to the exact reference digest used during fine-tuning." }
    ]),
  },
  {
    id: "aiml-mlsd-continuous-improvement-pipeline",
    title: "Continuous Improvement Pipeline",
    description: "Automate the cycle from feedback to root cause analysis to model updates.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "RotateCw",
    color: "bg-blue-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 52,
    content: emptyContent({ overview: "Automate the cycle from feedback to root cause analysis to model updates." }),
    article: toArticle([
      { type: 'paragraph', text: "A continuous improvement pipeline treats model quality as a time series, not a milestone. Production logs capture errors, latency, user corrections, and business outcomes; those signals feed triage dashboards and automated detectors that answer whether a regression is data drift, a bad rollout, or an emerging adversarial pattern. Root cause analysis for failures often spans multiple teams: bad answers may trace to retrieval staleness, tokenizer changes, or upstream feature pipelines rather than weights alone, so incident tooling should link model version, data snapshot IDs, and dependency health in one traceable graph." },
      { type: 'paragraph', text: "Automated retraining triggers balance responsiveness against noise. Simple thresholds on accuracy proxies can oscillate if sample sizes are small; better approaches combine sequential tests on holdout slices, drift statistics on input embeddings, and guardrails that require human approval for high-risk domains. Quality regression detection should include canary comparisons and shadow evaluations before full traffic adoption. Integrating A/B experiments ties model changes to causal estimates of revenue or engagement instead of proxy metrics that moved without user value." },
      { type: 'paragraph', text: "Improvement velocity metrics make the pipeline legible to leadership: median time from flagged issue to patched model in production, percentage of regressions caught pre-release, and cost per incremental quality point on benchmark suites. Architecture-wise, separate training orchestration from serving CI so long-running GPU jobs do not block hotfixes, but unify metadata so every production model references the datasets, code commits, and evaluation reports that justified promotion. The trade-off is process overhead versus chaos: lightweight teams move fast until untracked experiments make incidents unrecoverable; heavy gates slow iteration unless automation removes manual toil." },
      { type: 'qa', question: "What signals would you use to trigger an automated retrain versus opening a manual investigation?", category: "Operations", answer: "Automate when drift tests cross predefined thresholds on large, representative traffic, when rolling accuracy on labeled slices drops with statistical significance, and when dependencies such as a knowledge index refresh complete successfully. Prefer manual investigation for rare safety incidents, single-customer spikes, or ambiguous metric movement that could be instrumentation bugs. Always require human gates for regulated domains even if numbers look bad." },
      { type: 'qa', question: "How do you integrate A/B tests into a continuous delivery flow for models without contaminating experiment readouts?", category: "Architecture", answer: "Assign stable experiment keys at the edge, route deterministically, log exposures with model version and feature flags, and power analyses using preregistered metrics. Ensure training data from experiments does not leak labels across arms when computing automated rewards. Provide kill switches that revert assignment instantly and isolate infrastructure so one arm’s autoscaling does not starve another." },
      { type: 'qa', question: "How would you run root cause analysis for a sudden increase in toxic outputs?", category: "Methodology", answer: "Segment by geography, language, prompt topic, and model version to see if the spike correlates with a rollout or a traffic shift. Inspect retrieval corpora and tool outputs for poisoned documents, review recent data ingestion jobs, and compare token distributions against a baseline. Interview on-call changes to moderation filters, then design a corrective dataset or policy layer and validate on held-out toxic prompts before redeploying." }
    ]),
  },
  {
    id: "aiml-mlsd-knowledge-base-update-automation",
    title: "Knowledge Base Update Automation",
    description: "Keep knowledge bases current with automated ingestion and refresh.",
    difficulty: "Intermediate",
    progress: 0,
    icon: "BookOpen",
    color: "bg-cyan-600",
    section: "mlsd",
    track: 'aiml',
    prerequisites: [],
    estimatedMinutes: 8,
    order: 53,
    content: emptyContent({ overview: "Keep knowledge bases current with automated ingestion and refresh." }),
    article: toArticle([
      { type: 'paragraph', text: "Retrieval-augmented systems go stale the moment documentation, pricing, or policies change without the index noticing. Automated document ingestion pipelines watch sources such as CMS webhooks, git repositories, ticketing exports, and blob storage prefixes. Each detected change should enqueue normalization jobs that extract text, preserve provenance metadata, and apply access-control tags so multi-tenant indexes never leak restricted sections. Batch crawls still matter as a safety net when events are missed, but event-driven updates reduce time-to-freshness for critical pages." },
      { type: 'paragraph', text: "Change detection goes beyond file hashes: semantic diffing can skip re-embedding paragraphs that moved but did not edit, while structural edits in HTML might alter chunk boundaries enough to require full section re-chunking. Incremental re-embedding strategies embed only new or altered chunks and update vector stores with delete-insert semantics keyed by stable document and chunk identifiers. Stale content detection complements push updates by scoring last-reviewed dates, decaying trust for pages with conflicting numeric facts, or comparing live fetches to cached snapshots when legal requires periodic reaffirmation." },
      { type: 'paragraph', text: "Knowledge graph updates introduce entity resolution challenges: merging synonyms, deprecating nodes, and validating new edges against ontology rules. Freshness scoring helps rankers prefer recently verified facts without ignoring stable reference material. Scheduled jobs suit predictable publisher releases; event-driven workers suit bursty wiki edits. Reliability demands idempotent consumers, dead-letter queues for poison documents, and shadow indexing environments where new corpora are evaluated for retrieval precision and hallucination rate before the router switches production traffic." },
      { type: 'qa', question: "How would you architect incremental re-embedding when documents are large and edits are small?", category: "Architecture", answer: "Parse documents into stable chunk IDs based on headings or content hashes, diff at chunk granularity, and enqueue embedding jobs only for changed chunks while tombstoning removed spans in the vector index. Maintain a mapping table from chunk IDs to vector IDs in the store. For boundary shifts, detect overlap with neighboring chunks and re-embed affected windows to avoid orphaned partial text." },
      { type: 'qa', question: "What automation patterns balance freshness against cost for a million-document corpus?", category: "Automation", answer: "Tier sources by business criticality: high-value namespaces update in near real time with streaming workers, while archival tiers refresh weekly. Cache embedding models in warm pools, batch embed within latency budgets, and deduplicate identical boilerplate across pages. Use sampling-based audits to catch drift instead of re-embedding everything on a short cadence." },
      { type: 'qa', question: "What reliability issues appear when webhook-driven ingestion is the only update path?", category: "Reliability", answer: "Missed deliveries, duplicate events, and out-of-order updates can leave the index inconsistent unless consumers are idempotent with monotonic version vectors per document. Network partitions between the CMS and workers cause silent staleness unless reconciled by periodic full scans or cursor-based backfills. Always monitor lag percentiles and alert when any namespace exceeds freshness SLOs." }
    ]),
  }
];
